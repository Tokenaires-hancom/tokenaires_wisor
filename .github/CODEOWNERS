# 역할별 담당 영역 구분 없이 4명 전원이 저장소 전체를 공동 소유한다.
# PR을 열면 작성자 본인을 제외한 나머지에게 자동으로 리뷰 요청이 간다.
#
# limsojang-gif는 저장소 초대를 아직 수락하지 않았다. GitHub는 쓰기 권한이
# 있는 사용자만 CODEOWNERS로 자동 지정하므로, 초대가 수락되기 전까지는 이
# 사람에게 리뷰 요청이 자동으로 가지 않는다. 수락 후 별도 조치 없이 동작한다.
* @chrusterd @rainbow0291 @naemnaem99 @limsojang-gif

# 클린 코드 검수 시스템 자체(2026-08-18 설계)의 설정 파일은 chrusterd(게이트
# 키퍼)만 소유한다. 뒤에 나오는 규칙이 앞의 `*`보다 우선하므로, 이 경로들은
# branch protection의 "코드오너 리뷰 필수" 옵션과 합쳐지면 chrusterd 승인
# 없이는 병합할 수 없다 — 이 파일들을 아무나 조용히 완화하는 걸 막기 위해서다.
.claude/** @chrusterd
.githooks/** @chrusterd
CLAUDE.md @chrusterd
scripts/pr_checks/** @chrusterd
