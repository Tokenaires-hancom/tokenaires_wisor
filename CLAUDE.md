# CLAUDE.md — Wisor

> 이 파일은 저장소 어디서 작업하든 읽힙니다. 팀 전체가 공유하는 규칙만 둡니다.
> 영역별 상세 규칙은 각 디렉터리의 `CLAUDE.md`에 있습니다.

---

## 이 제품이 무엇인가

기업을 고르는 법을 가르치는 **투자 학습 서비스**입니다. 종목 추천 앱이 아닙니다.

핵심 원칙은 코드로 강제돼 있고, 어기는 변경은 테스트에서 막힙니다.

1. **투자 스타일 점수는 관찰과 확인 사항까지만 말하고, 매수·매도를 권하지 않는다.**

판단이 애매할 때는 이렇게 물으세요. **"이 변경이 사용자를 '살까 말까'로 데려가는가, '무엇을 확인해야 하는가'로 데려가는가?"** 앞쪽이면 하지 않습니다.

---

## 저장소 구조와 경계

```
wisor/
├─ apps/web/            Next.js 15 · 화면 전체          → apps/web/CLAUDE.md
├─ data-pipeline/       Python 배치 · 스타일 점수        → data-pipeline/CLAUDE.md
├─ supabase/schema.sql  사용자 데이터 스키마
└─ docs/                결정 기록
```

경계는 하나뿐이고, 이 경계는 절대 넘지 않습니다.

| 경계 | 규칙 |
|---|---|
| 화면 ↔ 재무데이터 | 화면은 `apps/web/lib/generated/scores.json`만 읽습니다. 재무 원천을 직접 만지지 않습니다. |

---

## 명령

```bash
# 점수 배치 (재무데이터 → scores.json)
cd data-pipeline && python run_batch.py && pytest -q

# 웹
cd apps/web && npm run dev
cd apps/web && npm run build      # 커밋 전 반드시 통과
```

**커밋 전 두 가지를 모두 통과해야 합니다.** 어느 영역을 고쳤든 전부 돌립니다. 점수 모델을 바꾸면 화면이 깨집니다.

---

## 클린 코드 5원칙

4명 다 비개발자입니다. 코드를 읽고 품질을 판단할 수 없고, 코드를 짜는 주체는 AI입니다. 그래서 "사람이 읽기 좋은가"가 아니라 **"AI가 다시 이 코드를 정확히 이해하고 고칠 수 있는가"**를 기준으로 삼습니다.

아래는 코드를 짤 때 지키는 규칙이자, `push` 전 검수의 판정 기준입니다.
**제목은 검수가 묻는 질문이고, 본문은 코드를 짤 때 지키는 지시입니다.**

넷은 "예"가 좋은 답인데 **4번만 "예"가 문제**입니다. 그래서 판정 기호는 언제나
답이 예냐 아니오냐가 아니라 **문제가 있냐 없냐**를 가리킵니다 — 4번에 ✅가 붙었으면
"처음 보는 방식이 없다"는 뜻입니다.

**1. 기능에 맞는 테스트가 있는가?**
기능을 만들면 테스트도 같이 만듭니다. 테스트로 증명하기 어려운 화면은, 무엇을 눈으로 확인하면 되는지 PR 본문에 적습니다. 통과/실패가 명확한 증거가 없으면 그 코드가 맞다고 말할 방법이 없습니다.

**2. 한 번에 한 가지만 고쳤는가?**
다른 영역까지 건드려야 하면 먼저 말합니다. 한 기능을 고치라고 시켰을 때 다른 곳이 깨지면 비개발자는 diff를 봐도 알아채지 못합니다. 모듈은 작게, 서로 독립적으로 유지합니다.

**3. 같은 일을 하는 코드가 한 곳에만 있는가?**
같은 로직이 두 곳에 있으면 나중에 AI가 한 곳만 고치고 나머지를 놓쳐서, 눈에 안 보이는 불일치가 생깁니다. 복붙하지 말고 함수로 뽑습니다. 일반적인 DRY보다 엄격하게 지킵니다.

**4. 처음 보는 방식이 들어갔는가?**
새 추상화나 영리한 트릭을 만들지 않습니다. 예측 가능한 흔한 패턴이 안전한 코드입니다 — AI가 나중에 이 코드를 다시 이해하고 고칠 확률이 높아집니다.

**5. 왜 그렇게 했는지가 코드 밖에 남았는가?**
주석보다 커밋 메시지와 PR 본문에 남깁니다. 다음에 AI에게 이 부분을 다시 시킬 때 넣어줄 맥락이 그것뿐입니다. 형식은 "브랜치와 커밋" 절을 따릅니다.

### 검수 등급

두 검수(`clean-check`·`whole-check`)가 이 기준으로 판정합니다. **여기가 유일한 기준이고, 스킬 파일은 이 절을 가리키기만 합니다** — 두 곳에 적어두면 한쪽만 고쳐져서 같은 코드가 회차마다 다르게 판정됩니다.

- **❌ — 이 문서에 근거가 있는 위반.** 어느 문장을 근거로 삼았는지 괄호에 인용합니다.
- **⚠️ — 근거가 문서에 없는 일반론, 또는 확신이 없는 지적.**
- **✅ — 확인했고 문제 없음.** 무엇을 보고 그렇게 판단했는지 함께 적습니다.
- **— 해당 없음.** 이번 변경에 그 원칙을 적용할 대상이 아예 없을 때.

근거 없이 ❌를 달지 않습니다. 비개발자는 애매한 지적을 판단할 수 없어서 무조건 "고쳐줘"라고 하게 되고, 필요 없는 수정이 또 파급을 만듭니다.

**✅에도 근거가 필요합니다.** "무엇을 보고 문제없다고 했나"를 못 적으면 ✅가 아니라 "해당 없음"입니다. 무엇과 무엇을 비교했는지, 몇 개를 셌는지처럼 **확인할 수 있는 사실**이어야 합니다. "이 저장소에서 이미 쓰던 방식입니다", "표준 표기입니다" 같은 문장은 근거가 아니라 빈칸 메우기입니다.

빈칸을 메우면 **진짜 지적 하나가 그럴듯한 문장 넷에 묻힙니다.** 코드를 못 읽는 사람은 어느 쪽이 알맹이인지 구별할 수단이 없습니다. 할 말이 없으면 "해당 없음"이라고 적는 편이 훨씬 정직하고 읽기 쉽습니다.

**"해당 없음"은 ⚠️도 대신합니다.** 적용 대상이 없는데 ⚠️를 달면 **있지도 않은 문제를 만들어내는 것**이고, 이쪽이 지어낸 ✅보다 나쁩니다 — 보고서에 "고칠 게 있다"고 적힌 셈이라 사람이 실제로 뭔가 고치려 들게 됩니다. 문서만 바꾼 변경에 "테스트가 없습니다"는 지적이 아니라 잡음입니다.

1번은 이 분기로 판정합니다.

```
기능 코드가 바뀌었나?
├ 아니오 (문서·설정만)                      → — 해당 없음
└ 예
   ├ 테스트가 있다                          → ✅ 어느 테스트인지 적는다
   ├ 테스트는 없지만 확인 방법을 PR에 적었다  → ✅ 무엇을 확인했는지 적는다
   └ 둘 다 없다                             → ⚠️ 또는 ❌
```

**문제가 없어도 항목을 전부 출력합니다.** 침묵하면 "깨끗함"인지 "검수를 안 했음"인지 구별되지 않습니다. "해당 없음"도 출력합니다 — 그것도 판정입니다.

**검수는 고치지 않습니다.** 지적만 하고 멈춥니다. 사람이 읽고 "3번 고쳐줘"라고 말하면 그때 고칩니다.

`git push`를 시도하면 훅이 한 번 막고, `.claude/skills/clean-check/SKILL.md`가 이 5개 항목을 각각 판정해서 알려줍니다. **막지 않습니다** — 지적을 보고 고칠지 그대로 갈지는 사람이 정합니다.

push 전에 `/code-review`를 돌리면 지적이 덜 나옵니다. 필수는 아닙니다.

이 검수는 **이번에 나갈 변경**만 봅니다. 저장소 전체가 지금 어떤 상태인지는
`.claude/skills/whole-check/SKILL.md`가 봅니다 — "전체 점검해줘"라고 하면 돌고,
결과는 `docs/audit/YYYY-MM-DD.md`에 남습니다. 자동으로 돌지 않습니다.

---

## 절대 하지 말 것

아래는 Claude가 기본값으로 자주 시도하지만 이 제품에서는 **틀린** 동작입니다.

- ❌ **없는 값을 0으로 채우기.** 판정할 데이터가 없으면 `fail`이 아니라 `unknown`입니다. `unknown`은 점수 계산의 분모에서도 빠집니다.
- ❌ **주식 화면에 빨강·초록 사용.** 두 색은 가격 방향을 뜻하는데 이 제품은 방향을 말하지 않습니다. 충족은 `--gold`, 미충족은 `--ochre`이고, 색만으로 의미를 전달하지 않도록 채움·빗금·점선을 함께 씁니다. 포인트 색 `--wine`은 붉은 계열이라 버튼과 배우기 화면 UI에만 쓰고, 충족 표시에는 쓰지 않습니다.
- ❌ **`scores.json` 직접 수정.** 배치를 다시 돌립니다.
- ❌ **컴포넌트에서 `localStorage` 직접 호출.** 저장은 `apps/web/lib/store.ts`를 거칩니다. Supabase 교체 지점이기 때문입니다. 이 함수들은 전부 `Promise`를 돌려줍니다.
- ❌ **클라이언트 컴포넌트에서 `lib/scores.ts` import.** scores.json 전체가 브라우저 번들에 실립니다. 타입과 라벨은 `lib/scores.types.ts`에서 가져오고, 데이터는 서버 컴포넌트에서 props로 내려보냅니다.
- ❌ **의존성 추가.** 새 패키지가 정말 필요하면 먼저 물어보세요. 지금 웹은 Next·React뿐이고 그 상태를 유지합니다.
- ❌ **"~하면 좋습니다", "지금이 기회입니다" 류 UI 문구.** 사용자에게 보이는 문장은 관찰과 확인 사항까지입니다.

---

## 사용자에게 보이는 문장 규칙

| 쓰지 않음 | 대신 |
|---|---|
| 지금 사야 합니다 | 8개 기준 중 6개에 부합합니다 |
| 곧 오를 종목입니다 | 현금흐름은 강하지만 가치평가의 할인 폭은 크지 않습니다 |
| 버핏이 좋아할 종목이라 안전합니다 | 투자 전 최근 성장 둔화를 추가로 확인해야 합니다 |
| 골든크로스 발생, 상승 전환 | 단기선이 장기선을 위로 지나는 모습이 보입니다. 이 현상을 골든크로스라고 부릅니다 |
| 현재 기준 | 2026-07-29 종가 기준 |

"현재"라고 쓰지 않고 날짜를 씁니다. 모든 점수 화면에 가격 기준일·재무 기준일·점수 모델 버전이 함께 나가야 합니다.

금지어 목록은 `data-pipeline/wisor_data/styles/base.py`의 `BANNED_PHRASES`에 있고 **실행되는 코드**입니다.

---

## 변경이 번지는 지점

한 곳을 고치면 반드시 함께 봐야 하는 것들입니다. 이걸 놓쳐서 나는 버그가 가장 많습니다.

**점수 기준을 추가·삭제하면**
→ 기준 개수를 언급한 UI 문구를 함께 고칩니다(버핏은 "8개 기준"으로 고정돼 있습니다)
→ `tests/test_scoring.py::test_criteria_count_is_eight_for_buffett` 수정
→ `run_batch.py` 재실행 → `scores.json` 커밋
→ 기준 막대의 칸 수가 바뀌므로 스크리너 화면 확인

**`lib/store.ts`의 함수 시그니처를 바꾸면**
→ Supabase 교체 계획과 어긋나지 않는지 2번과 확인합니다
→ 동기 함수로 되돌리지 않습니다. 비동기로 맞춰 둔 이유가 그 교체입니다

**클라이언트 컴포넌트를 새로 만들면**
→ `lib/scores.ts`를 import하지 않았는지 확인합니다
→ `npm run build` 후 `grep -rl "evEbitMedian5y" .next/static/`가 비어 있어야 합니다

---

## 4인 역할과 소유권

| | 담당 | 주 작업 디렉터리 |
|---|---|---|
| **1번** | 제품·UX·프론트엔드 | `apps/web/app`, `apps/web/components`, `apps/web/content` |
| **2번** | 백엔드·Supabase·통합 | `supabase/`, `apps/web/lib/store.ts`, 배포·환경변수 |
| **3번** | 재무데이터·스타일 점수 | `data-pipeline/` |
| **4번** | (차트 교육·비전 LLM 기능 제거로 공석 — 다음 담당 재배정 필요) | — |

**남의 영역 파일을 고쳐야 하면 직접 고치지 말고 이슈로 넘깁니다.** 예외는 명백한 오타와 타입 오류입니다.

리뷰 담당(설계 가이드 12장):

| 변경 영역 | 작성 | 리뷰 |
|---|---|---|
| 스타일 점수 | 3번 | 1번 |
| 스크리너·종목 상세 화면 | 1번 | 2번·3번 |
| 학습노트 | 2번 | 1번 |
| **데이터 검수** | 3번 | **전원** |

---

## 브랜치와 커밋

```
main                 배포 가능한 상태만
  └─ feat/<영역>-<내용>     feat/screener-filter
  └─ fix/<영역>-<내용>      fix/screener-filter-timeout
  └─ data/<내용>           data/graham-1.0-promotion
```

**커밋 메시지와 PR 제목은 `type: 설명` 형식을 씁니다.** `type`은 브랜치 접두사와 같은 집합입니다 — `feat`(기능) · `fix`(버그) · `docs`(문서) · `chore`(잡일) · `data`(데이터 승격). 설명은 한국어 한 줄. 무엇을 바꿨는지가 아니라 **왜 바꿨는지**를 씁니다. 필요하면 본문을 이어서 씁니다.

```
fix: 스타일 점수에서 unknown을 분모에서 제외

데이터가 없는 기준을 미충족과 같이 취급하면 재무 공시가 늦은 기업이
구조적으로 불리해진다. 판정 가능한 기준끼리만 비교하도록 바꿨다.
```

PR에는 세 가지를 적습니다. 무엇을 바꿨는지 / 왜 / 어떻게 확인했는지.

PR을 열면 `.github/workflows/check.yml`이 테스트·빌드·타입 검사·재무데이터 유출 검사를 돌립니다. **이 넷은 실패하면 병합이 막힙니다.** 제목·본문 형식은 기계가 보지 않습니다 — push 전 검수의 5번 항목이 봅니다.

---

## Claude Code 사용 규칙

**Plan Mode를 먼저 켜야 하는 경우**
- 두 개 이상의 디렉터리를 건드리는 변경
- 점수 모델, 필터 규칙, 스키마 변경
- "이 기능 만들어줘" 수준의 넓은 요청

한 파일 안의 좁은 수정은 바로 진행해도 됩니다.

**작업 시작할 때**
작업할 디렉터리로 먼저 들어가서 세션을 시작하세요. 그 디렉터리의 `CLAUDE.md`가 함께 읽힙니다. 루트에서 시작하면 남의 영역 규칙까지 매번 컨텍스트에 들어갑니다.

**서브에이전트를 쓸 만한 곳**
- 점수 결과를 실제 공시와 대조하는 검수 작업

**작업이 끝났다고 말하기 전에**
해당 영역의 테스트와 `npm run build`를 실제로 돌린 결과를 붙입니다. "통과할 것으로 보입니다"는 통과가 아닙니다.

**공유 훅**
`.claude/settings.json`에 훅이 하나 걸려 있습니다. `git push`를 시도하면 한 번 막고 클린 코드 5원칙 검수를 거치게 합니다("클린 코드 5원칙" 절 참고). 검수 결과 자체는 아무것도 막지 않습니다.

개인 설정은 `.claude/settings.local.json`에 두세요. gitignore돼 있습니다.

---

## 예시 데이터에 대해

지금 저장소의 재무수치는 **손으로 만든 예시 데이터**입니다(`data-pipeline/data/universe_sample.json`). 실제 기업 재무제표가 아닙니다.

- 이 데이터를 근거로 종목에 대한 판단을 쓰지 않습니다
- 화면의 "예시 데이터" 표시를 지우지 않습니다
- 실데이터가 붙으면 `dataSource`가 `vendor`로 바뀌며 표시가 자동으로 사라집니다

---

## 막혔을 때

- 기획 의도가 불분명하면 → `docs/`와 원본 기획서를 먼저 확인하고, 그래도 모르면 추측하지 말고 물어봅니다
- 원칙과 요청이 충돌하면 → 원칙이 이깁니다. 충돌한다는 사실을 먼저 말합니다
- 왜 이렇게 짰는지 모르겠으면 → 대부분 이유가 주석에 있습니다. 없으면 그 자체가 버그 신호입니다
