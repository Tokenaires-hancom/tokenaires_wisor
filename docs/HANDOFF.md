# 인계 노트

## 2026-08-20 · Codex · 학습노트 저장 안내 수정

- 종목 상세의 학습노트 안내를 실제 인증 저장 흐름에 맞춰, 비회원 임시 저장 후 로그인·가입 시 계정 기록과 병합된다고 수정했습니다.
- 이 브랜치는 `feat/auth-learning-storage` 위에 쌓여 있습니다.
## 2026-08-21 · Claude · 병합에서 사라진 채점 안내 표 스타일 복구

- `/learn/scoring`의 단계 목록(`.scoring-steps`)과 철학별 채점 종목 수 비교표(`.coverage-table`) CSS가 `dc71dd4` 병합에서 통째로 사라져 두 화면이 브라우저 기본 스타일로 나오고 있었습니다. 마크업은 그대로 두 클래스를 쓰고 있어 규칙만 되살렸습니다.
- 복구 전 계산된 스타일은 `list-style: decimal` · `padding-left: 40px`, 표는 `border-collapse: separate` · `td padding 1px`으로 열이 어긋났습니다. 복구 후 단계는 구분선으로 나뉘고 표는 전폭·숫자 우측정렬·행 구분선을 되찾습니다.
- 새 셀렉터를 만들지 않고 유실 이전 규칙을 그대로 복원했습니다. 브라우저에서 `/learn/scoring`의 계산된 스타일로 복구 전후를 확인했습니다.
## 2026-08-21 · Claude · 내 학습을 마이페이지 장부 화면으로 재배치

- `/me`를 `my-page-shell` 사이드바와 본문 두 단으로 바꿨습니다. 메뉴는 `내 학습`·`관심 종목`·`계정 설정` 세 구획이고 각각 앵커 섹션으로 이동합니다. 760px 이하에서는 메뉴가 본문 위로 올라갑니다.
- 복습 기록을 `복습 일정 / 90일 뒤 다시 보는 기록` 섹션으로 분리하고, 볼 기록이 없을 때도 섹션과 안내 문구가 남도록 했습니다. 이전에는 기록이 없으면 섹션째 사라져 기능 자체가 없는 것처럼 보였습니다.
- 관심 종목과 계정 설정은 develop이 이미 넣은 `AccountSettings`와 계정 삭제 흐름을 그대로 쓰고, 앵커 섹션으로만 감쌌습니다.
- 계정·인증 화면 CSS를 이 구조에 맞춘 한 벌로 교체했습니다. develop의 `계정과 학습 기록 저장` 블록과 함께 두면 같은 셀렉터가 57건 겹치기 때문에 대체하는 형태입니다.
- 컴포넌트가 쓰는 `my-page-*`·`account-*`·`auth-*`·`watch-*`·`quiz-*` 클래스 67개가 모두 정의돼 있고 과잉 중복 정의가 0건인 것을 확인했습니다. 브라우저로 `/me`(사이드바 216px + 본문 684px, 여섯 섹션)와 `/login` 렌더링을 확인했습니다.

## 2026-08-20 · Codex · 인증·학습 저장 문서 동기화

- 서비스 기획서, 시스템 설계도, 유저 시나리오를 `feat/auth-learning-storage`의 실제 구현에 맞췄습니다.
- 비회원 localStorage 임시 기록, 로그인 뒤 Supabase 병합, 사용자별 RLS 저장 흐름과 아직 필요한 원격 마이그레이션·테스트 계정 검증을 구분해 기록했습니다.
- 이 문서 브랜치는 인증 구현 브랜치 위에 쌓여 있으므로, 인증 변경이 먼저 반영된 뒤 병합합니다.

## 2026-08-20 · Codex · 자동 점수 배치 Environment 연결

- `scores` GitHub Actions job에 `environment: env`를 지정해 `env` Environment에 개별 등록된 `TOSS_INVEST_CLIENT_ID`, `TOSS_INVEST_CLIENT_SECRET`, `WISOR_SEC_USER_AGENT` Secret을 읽도록 연결했습니다.
- 예약 주기와 배치 명령은 바꾸지 않았습니다. `develop` 머지 후 `prices` 수동 실행으로 자격증명과 실제 갱신을 확인합니다.

## 2026-08-19 · Codex · 기획서·시스템 설계도·유저 시나리오 교차 검증

- 프로젝트 전반의 현재 코드, `scores.json`, 배포 설정, 저장소 경계, 화면 링크를 기준으로 `docs/service-plan-wisor-2026-08-18.md`, `docs/system-design.md`, `docs/user-scenarios.md`를 교차 확인했습니다. 세 문서는 현재 구현과 미래 계획을 분리하며, 매수·매도 판단을 기능으로 서술하지 않습니다.
- 서비스 기획서의 린치 기준 수를 실제 모델과 `scores.json`에 맞춰 6개에서 5개로 바로잡고, 제거된 주식 기본개념 단원과 전용 초보 시나리오를 문서에서도 정리했습니다. 시스템 설계도의 데이터 흐름 절 번호를 4.5에서 4.4로, 클라이언트 컴포넌트 수를 16개에서 실제 15개로 고쳤습니다.
- 웹 테스트 87개와 데이터 파이프라인 테스트 128개가 통과했고, 프로덕션 빌드는 435개 정적 페이지를 생성했습니다.
- `persona_explain`은 79개 중 78개가 통과하고 1개가 실패했습니다. 원인은 `scores.json`에 있는 `magicFormulaRoc`가 `persona_explain/scores_source.py`의 `SCORES_KEY_TO_METRIC`에 없는 계약 불일치입니다. 요청 범위가 문서 작성이라 코드는 변경하지 않고 세 문서에 현재 상태와 후속 작업으로 기록했습니다.
- 루트 지침의 기본 `python run_batch.py`는 실데이터 380종목 파일을 예시 12종목으로 덮을 수 있어 실행하지 않았습니다. 검증에는 기존 산출물을 읽는 테스트만 사용했습니다.
## 2026-08-19 · Codex · 마법공식 스크리너 설명 추가

- `/screener/greenblatt`에 마법공식이 자본수익률과 이익수익률을 각각 순위 매긴 뒤 합산하는 방식임을 보여주는 전용 설명 블록을 추가했습니다. 후속 요청으로 그린블랫이 그레이엄의 가격 원칙과 버핏의 기업 품질 원칙을 결합하고, 오랫동안 투자와 강의에 쓴 방식을 2000년대 초 컴퓨터로 검증해 두 순위 규칙으로 단순화한 탄생 배경도 덧붙였습니다.
- 질 순위, 가격 순위, 합산 순위의 세 단계와 제외 업종, 순위 해석 주의점을 기존 골드·잉크 디자인 체계로 표현했습니다. 다른 스크리너 화면에는 표시되지 않습니다.
- `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성). 실행 중인 그린블랫 페이지에는 설명이 있고 버핏 페이지에는 표시되지 않는 것을 HTML 응답으로 확인했습니다. 이번 후속 문구 변경에서도 테스트와 빌드를 다시 통과했으며, 연결 가능한 브라우저가 없어 자동 화면 캡처는 진행하지 못했습니다.
## 2026-08-19 · Codex · 비버핏 커리큘럼 반응 이미지 제거

- `/learn/masters/soros/3`을 포함한 비버핏 대가의 장별 커리큘럼에서 버핏 반응 이미지가 대체 표시되던 로직을 제거했습니다.
- 대가별 메인 화면의 전용 `stand.webp`는 유지하고, `mood`가 요청된 장별 화면에서는 해당 대가의 반응 자산이 없으면 `MasterCharacter`가 아무것도 렌더링하지 않습니다. 버핏 커리큘럼의 기존 반응 이미지는 유지됩니다.
- `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성). 비버핏 30개 장별 URL에서 버핏 이미지 경로가 없고 버핏 5개 장에는 유지되는지 확인했습니다. 연결 가능한 브라우저가 없어 자동 화면 캡처는 진행하지 못했습니다.

## 2026-08-19 · Codex · 일곱 대가 전신 캐릭터 통일

- 버핏 `stand.webp`의 2:3 전신 비율·안내 자세·남색 정장·캐릭터 화풍을 기준으로, 기존 원형 초상화의 얼굴 특징을 반영한 그레이엄·린치·피셔·그린블랫·막스·소로스 전용 정지 이미지를 생성했습니다.
- 새 자산은 `public/characters/<id>/stand.webp`에 버핏과 같은 500×750 크기로 저장했습니다. 생성기의 투명 배경이 체크무늬 픽셀로 출력되어, 실제 화면의 순백 배경에 맞춘 흰 배경 버전으로 보정했습니다.
- `STAND_READY`에는 일곱 명을 모두 등록했고, 반응 애니메이션을 가진 버핏만 `MOOD_READY`로 따로 관리합니다.
- 최종 WebP 여섯 장을 직접 열어 머리·안경·얼굴형, 전신 크롭, 배경을 확인했습니다. `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성), 여섯 대가 화면과 이미지 경로 모두 200 응답 및 각 화면의 전용 경로 렌더링을 확인했습니다. 연결 가능한 브라우저가 없어 자동 화면 캡처는 진행하지 못했습니다.

## 2026-08-19 · Codex · 배우기 워런 버핏 이미지 통일

- 처음에는 `/learn`의 워런 버핏 카드가 사용하는 `characters/buffett/main.png`를 사용자 제공 이미지로 교체했지만, 다른 투자자 카드와 화풍·크기가 달라 후속 요청에서 철회했습니다.
- 최종 구현은 기존 제작 세트의 `public/investors/buffett.png`를 사용합니다. `MasterGrid`의 버핏 전신 이미지 예외와 전용 반응형 크롭 CSS를 제거해 일곱 명 모두 같은 256×256 원형 초상화와 `master-card-portrait` 규칙을 공유합니다.
- `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성). 인앱 브라우저에 연결 가능한 브라우저가 없어 자동 스크린샷 검토는 진행하지 못했습니다.
## 2026-08-19 · Codex · 퀴즈 결과를 투자 대가별로 그룹화

- `/me`의 퀴즈 결과를 학습 여정의 투자 대가 순서로 묶고, 각 묶음 안에서는 장 순서대로 정렬했습니다.
- 대가별 헤더에 푼 챕터 수와 합산 정답 수를 표시하며, 기본적으로 모두 펼쳐 두되 네이티브 `details`로 개별 대가 결과를 접고 펼칠 수 있습니다.
- 결과 카드의 중복 대가명은 제거하고 왼쪽 번호를 실제 장 번호로 바꿨습니다. 기존 전체 요약과 챕터 다시 보기 링크는 유지합니다.
- `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성). 연결 가능한 인앱 브라우저가 없어 스크린샷 검증은 진행하지 못했습니다.

## 2026-08-19 · Codex · 내 학습 관심종목 정보 카드 재설계

- `/me`의 이름·티커만 보이던 관심종목 목록을 기업 파일 형태의 정보 카드로 교체했습니다.
- 카드에 업종, 종가, 시가총액, 380종목 유니버스 내 시가총액 순위, 가격 기준일, 학습노트 유무를 표시하고 기업 상세로 이동하는 행동을 명시했습니다. 가격 등락과 투자철학 점수는 넣지 않았습니다.
- 서버 컴포넌트는 380종목의 카드용 요약 필드만 클라이언트에 전달하며 전체 점수·재무지표는 전달하지 않습니다. 820px 이하 1열, 520px 이하 지표 2열 배치를 적용했습니다.
- `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성), `/me` 200 응답과 클라이언트 번들 경계를 확인했습니다. 연결 가능한 인앱 브라우저가 없어 스크린샷 검증은 진행하지 못했습니다.

## 2026-08-19 · Codex · 내 학습 퀴즈 결과 가독성 개선

- `/me`의 단순 문장형 퀴즈 결과 목록을 전체 정답 수, 완료·복습 챕터 요약, 개별 챕터 결과 카드로 재구성했습니다.
- 각 카드에서 투자 대가·장 제목·정답 수·응시일·다시 보기 링크를 분리하고, 모두 맞힌 결과는 골드 단색·실선, 다시 볼 결과는 중립 배경·점선과 문구로 구분했습니다.
- 결과는 대가와 장 순서로 정렬하며 기존 `quizResults` 저장 구조는 바꾸지 않았습니다. 820px 이하 한 열, 520px 이하 압축 배치를 추가했습니다.
- `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성). 연결 가능한 브라우저가 없어 스크린샷 검증은 진행하지 못했습니다.

## 2026-08-19 · Codex · 주식 기본개념 단원 완전 제거

- `/me`에서 주식 기본개념 진도 카드를 제거하고, 예전 브라우저 저장소에 남은 `basics:*` 퀴즈 결과도 더 이상 표시하지 않습니다.
- 진입점이 사라진 기본개념 모달 컴포넌트와 5단원 콘텐츠, 전용 분석 이벤트·CSS를 삭제했습니다. 투자 대가 학습과 공유되는 퀴즈·단계 이동 스타일은 유지합니다.
- 현재 제품을 설명하는 서비스 기획서와 유저 시나리오의 대응 변경은 별도 `docs/sync-product-specs` 브랜치로 분리했습니다.
- `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성). 개발 서버의 `/me` 응답에서 기본개념 문구가 사라졌고, 이전 `/learn/basics` 및 단원 URL이 404인 것을 확인했습니다. 연결 가능한 브라우저가 없어 자동 스크린샷 검증은 진행하지 못했습니다.
## 2026-08-19 · Codex · 점수로 만들지 않은 철학 기본 접힘
## 2026-08-19 · Codex · 챗봇 헤더와 대가 선택 UI 변경

- 챗봇 패널의 `해설 관점`과 `숫자는 이 철학으로 읽습니다`를 하나의 제목 `투자 대가에게 묻기`로 교체했습니다.
- 버핏·그레이엄·린치·그린블랫 선택 버튼을 제목 바로 아래의 4열 초상 탭으로 재설계하고, 기존 `public/investors/` 이미지를 재사용했습니다. 선택 상태는 골드 받침·진한 테두리·체크 표식과 `aria-pressed`로 구분합니다.
- 좁은 화면에서 초상과 이름을 한 단계 줄이고, 패널 본문은 flex 레이아웃으로 바꿔 채팅 로그가 남은 높이를 사용하도록 했습니다. `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성). 연결 가능한 브라우저가 없어 스크린샷 검증은 진행하지 못했습니다.
## 2026-08-19 · Codex · 모든 장별 커리큘럼 캐릭터 제거

- `/screener/[style]` 상단의 `점수로 만들지 않은 철학` 안내를 네이티브 `details/summary`로 바꿔 기본 닫힘 처리했습니다. 제목 행을 클릭하거나 키보드로 열면 필립 피셔·하워드 막스·조지 소로스의 상세 이유가 표시됩니다.
- 기존 색상·테두리 체계를 유지하면서 열림 상태 화살표, 키보드 포커스, 모바일 여백을 추가했습니다.
- `npm test` 87개 통과, `npm run build` 성공(435개 정적 페이지 생성). localhost HTML 응답에서 `details`에 `open` 속성이 없고 세 철학 내용이 포함되는 것을 확인했습니다. 이 세션에는 연결 가능한 인앱 브라우저가 없어 스크린샷 검증은 하지 못했습니다.

## 2026-08-18 · Claude Code · 유저 시나리오 문서 작성

- `docs/user-scenarios.md`를 추가했습니다. `system-design.md`가 "무엇이 어디서 도는가"를 다루므로, 이 문서는 **"누가 어떤 순서로 화면을 지나가는가"**만 다룹니다. 저장소 내부용이며 기준은 `a41fb3b` **+ 미커밋 작업 트리**입니다 — S4(`app/learn/compare/page.tsx`)와 S1의 두 렌즈 서술(`components/StockLenses.tsx`)이 아직 커밋되지 않은 변경에 걸려 있어, 커밋만 체크아웃하면 문서대로 보이지 않습니다.
- **등장인물을 새로 만들지 않았습니다.** 기획서 §10.1의 네 세그먼트를 우선순위 그대로 씁니다. 두 문서가 사용자를 다르게 정의하기 시작하면 어느 쪽도 근거가 되지 못하기 때문입니다.
- 시나리오는 7개이고 각각 여섯 칸(상황 / 진입 / 화면 순서 / 시스템에서 일어나는 일 / 끝나는 지점 / 막히는 지점)으로 씁니다. **일곱 개 전부 확인 사항에서 끝납니다.** 매수·매도 판단으로 끝나도록 화면을 이어붙일 수 있는 경로는 찾지 못했고, 그 사실을 5장에 적었습니다.
- **작성 도중 시장 심리 기능이 제거되어(바로 아래 항목) 해당 시나리오를 통째로 뺐습니다.** 없는 화면의 시나리오는 쓰지 않는다는 판단이고, 빠졌다는 사실과 이유를 머리말과 4장에 남겼습니다. Nav 항목·페이지 수(435)·테스트 수도 제거 후 상태로 맞췄습니다.
- 시나리오 × 화면 커버리지 표를 넣었습니다. 이 표에서 **`/learn/compare`로 들어가는 링크가 저장소 어디에도 없다**는 것이 나왔습니다. 비교표 자체는 `StyleComparisonTable`이 `/learn` 하단에 임베드돼 있어 사용자가 볼 수 있고, 도달 불가능한 것은 **그 라우트에만 있는 `CROSS_EXERCISES` 최종 기록 문항**입니다. 커버리지 표에 `◐`로 표시했습니다. 시나리오를 지어내 칸을 채우지 않았습니다.
- 같은 표에서 `/learn/sources`가 `/learn`과 대가 개요에서만 들어가고, 정작 근거를 따지는 사용자가 있는 `/learn/scoring`에서는 갈 수 없다는 것도 나왔습니다. 스크리너 ↔ `/learn/scoring`은 `CoverageTable`을 통해 양방향으로 이어져 있습니다.
- **"아직 시나리오가 성립하지 않는 것" 절을 따로 뒀습니다.** 기기 간 이어보기(localStorage뿐, Supabase 미연결), 3개월 재노출 알림(`/me` 방문 시에만 계산되고 푸시 없음), 계정, 종목 검색(챗봇 안에만 있음), 점수 변화 알림이 여기 들어갑니다. 문서에 그럴듯하게 써 두면 없는 기능이 있는 것처럼 읽히므로 시나리오 본문에서 뺐습니다. 피셔·막스·소로스 미점수화만 성격이 달라(미완성이 아니라 결정) 별도로 표시했습니다.
- 불일치 두 건을 5장에 기록하고 해당 파일은 고치지 않았습니다. **어느 쪽이 오래됐는지까지 확인해서 적었습니다.**
  - **Lynch 기준 수** — 기획서 §4.2는 6개, `scores.json`은 5개인데, 모델 코드 `data-pipeline/wisor_data/styles/lynch.py`의 `CRITERIA`도 **5개**였습니다. 즉 `scores.json`이 낡은 게 아니라 **기획서가 낡았습니다.** 배치 재실행이나 기준 개수 변경 파급(CLAUDE.md의 UI 문구·테스트·`run_batch.py`·기준 막대 칸 수)은 필요 없고, 문서 한 줄을 고치면 됩니다. 다른 세 모델(버핏 8·그레이엄 6·그린블랫 2)은 코드와 JSON이 일치합니다. **고칠 사람은 3번이 아니라 기획서 담당입니다.**
  - **`/learn/compare` 링크 부재** — 이쪽은 문서가 아니라 저장소가 낡았습니다. `--include`를 `.ts`·`.tsx`·`.mjs`·`.json`까지 넓혀 찾아도 소스에는 링크가 없고, 걸리는 것은 빌드 산출물(`.next`·`.next-stale-*`)과 `StyleComparisonTable`의 주석 한 줄뿐입니다.
- 수치는 두 갈래로 나눠 인용했습니다. 380종목·312/289/250/244·버핏 8개 기준·가격 기준일 2026-08-12·AAPL의 판정 7개 중 충족 6개와 `이자 감당력` `unknown`은 `scores.json`에서 직접 읽었고, 198문항·435 페이지는 이 노트의 검증 기록을, 35장은 기획서 §4.1을 인용했습니다. 실제 기업 판정을 적은 곳은 AAPL 한 곳뿐이고 기억이 아니라 파일에서 읽은 값입니다.
- 검증: 문서만 추가·수정했고 코드 변경이 없어 `npm run build`는 돌리지 않았습니다. 확인한 것은 문서가 인용한 경로가 실제로 존재하는지와 수치가 `scores.json`·이 노트와 맞는지입니다.

## 2026-08-18 · Codex · 시장 심리 기능 제거

- 아직 푸시되지 않았던 시장 심리 관련 9개 커밋을 Git 이력에서 제거하고 브랜치를 `upstream/develop`로 되돌렸습니다.
- `/learn/market-sentiment` 화면, 계산 모듈·테스트, UI 컴포넌트, 전용 CSS, 데이터 수집기와 생성 JSON, 설계·구현 계획을 제거했습니다.
- 내비게이션·하워드 막스 화면·README·시스템 설계도에서 기능 진입점과 운영 설명을 제거했습니다. 다른 미커밋 작업은 유지했습니다.
- 검증: data-pipeline 128 pass, web 87 pass, `npm run build` 435/435 정적 페이지 성공. 빌드 경로 목록에 `/learn/market-sentiment`가 없음을 확인했습니다.

## 2026-08-18 · Claude Code · 외부 발표용 서비스 기획서 작성

- `docs/service-plan-wisor-2026-08-18.md`를 추가했습니다. 용도는 데모데이·부트캠프 발표이며 형식은 마크다운입니다. 발표 성격이 정해지면서 조달 규모 항목은 뺐습니다.
- 제품·기술 수치는 전부 저장소에서 직접 확인했습니다. 380종목·517 유니버스, 모델별 점수 산출 종목 수(312/289/250/244), 확인 문항 198개, 금지 표현 11개.
- 시장·경쟁은 웹 조사로 채웠습니다. Validea가 가장 가까운 유사 서비스입니다 — 12개 대가 모델로 종목을 채점하지만 공개 화면은 모델별 백분율까지이고 커리큘럼이 없습니다. 이에 따라 차별화 서술을 "근거를 공개한다"에서 "도구와 교재가 같은 화면에 있다"로 바꿨습니다.
- 규제는 두 갈래로 정리했습니다. 유사투자자문업(13.1)은 성립 요건 중 '고객으로부터 받는 대가'가 없어 해당하지 않고, 대신 금소법 광고 규제(13.3)가 적용됩니다. 둘 다 법률 의견이 아니라는 점을 문서에 명시했습니다.
- **수익모델을 노출 기반 증권사 광고로 정했습니다.** 사용자 과금 없음, 계좌개설·거래 연동 과금 없음. 과금 단위를 노출 정액으로 고른 이유가 핵심입니다 — 수익이 거래량이 아니라 학습 트래픽에 비례해야 "덜 거래하라"는 제품과 충돌하지 않고(8.1), 동시에 금융위가 중개 판단 기준으로 삼는 '판매실적 연동 수수료'를 피합니다.
- 이에 따라 14장을 '결정 필요 목록'에서 '수익모델과 남은 결정'으로 다시 썼고, 여섯 곳이 함께 바뀌었습니다. 8.1(증권사 앱 비판에 대한 선제 방어), 9.3(깨지는 조건 2개 추가), 10.3(무료가 임시가 아니라 전략), 12.1(분석 도구 연결을 최우선으로 승격), 13.1(유료화 분기점 → 요건 불성립), **13.3 광고 규제 신설**.
- 7.2 시장 규모는 유료 구독 전제였던 계산을 '시장의 지불 의사 크기'로 재정의하고, Wisor 매출은 `트래픽 × CPM`이며 CPM을 확인하지 못해 계산하지 않았다고 명시했습니다.
- 광고를 종목 점수 화면에 붙이지 말자는 규칙은 담당자 판단으로 넣지 않았습니다. 위험은 화면이 아니라 광고와 종목의 근접성에 있어서, '화면에 표시된 특정 종목·매매 행동과 연결되는 광고는 싣지 않는다'는 준수 조건 한 줄로만 남겼습니다.
- 조달 규모는 발표 성격이 데모데이·부트캠프라 삭제했습니다. 광고 단가·현재 사용자 수·광고주 접촉 여부는 여전히 미정이며 14.3에 이유와 함께 남겼습니다.
- Validea 요금을 확인해 비교표의 마지막 빈칸을 채웠습니다(Standard 분기 $74.95 ≈ 연 45만 원, Pro 연 약 162만 원, Institutional 연 $5,000). 가장 가까운 경쟁자가 대가 모델 채점만으로 연 45만 원을 받는다는 건 수요의 지불 의사를 보여주는 동시에 한국 개인 학습자에게는 진입 불가라는 뜻이라, 8.1에 그 해석을 붙였습니다.
- **웹과 챗봇 모두 Render에 배포됐고 라이브에서 연결까지 확인했습니다.** 웹 `wisor-w0fu.onrender.com`, 챗봇 API `tokenaires-wisor.onrender.com`. 챗봇 `/health`는 `status: ok` · 모델 `openai/gpt-5.4-mini` · 기업 380개 로드로 응답하고, 웹의 `/api/persona/health`도 정상입니다. 기획서 머리말·4.3·11.1·11.2·11.3·12.1·부록 A.1과 README에 반영했습니다.
- 확인 과정에서 **Netlify 쪽 예전 배포**(`tokenaires-wisor.netlify.app`)는 `/api/persona/health`가 500을 내는 것을 봤습니다. `netlify.toml`에 `PERSONA_API_ORIGIN`이 없어 기본값 `127.0.0.1:8000`이 박힌 탓입니다. 운영은 Render이므로 서비스에는 영향이 없지만, **저장소에 Netlify 설정만 있고 Render 설정은 플랫폼 UI에만 있어 저장소만 보면 배포처를 잘못 압니다.** `render.yaml`을 넣든 `netlify.toml`을 정리하든 배포 담당(2번) 판단이 필요해 문서에만 적었습니다.
- **챗봇 콜드스타트를 예열로 해결했습니다.** `PersonaChatFab`이 `ensureHealth()`를 `onToggle()` 안에서만 불러서, 사용자가 챗봇 버튼을 누른 그 순간에 잠든 서버를 처음 깨우는 구조였습니다. 마운트 시 `getHealth()`를 한 번 던지는 `useEffect`를 추가해 화면을 읽는 동안 깨어나도록 했습니다. 실패는 `console.debug`로 삼키고 화면에 영향을 주지 않으며, 열 때 기존 `ensureHealth`가 다시 확인합니다. 페르소나 목록도 함께 미리 채웁니다.
- 배포를 한 서비스로 합치는 안은 택하지 않았습니다. 웹은 Node(`next start`), 챗봇은 Python(`ThreadingHTTPServer`)이라 Render 한 서비스에 넣으려면 두 런타임을 담은 Dockerfile과 프로세스 매니저가 필요합니다. 체감 문제는 예열로 사라지므로 배포 구조는 그대로 뒀고, 기획서 11.2에 분리 이유를 런타임 차이로 명시했습니다.
- 검증: web 91 pass, `npx tsc --noEmit` 통과, `npm run build` 435/435 성공, `grep -rl "evEbitMedian5y" .next/static/` 비어 있음(번들 경계 유지).
- **기획 단계 피드백 6건에 대한 대응을 3.2로, 데이터 계약을 3.3으로 추가했습니다.** 3장 하위로 넣어 기존 절 번호와 상호참조는 그대로 유지됩니다.
- 근거는 전부 저장소에서 확인했습니다. 1차 자료 22건, 출처 각주 118개(원문 66·정리 30·창작 22, `SOURCE_KINDS`로 태깅되고 `validate`가 빌드에서 검사), `DuoQuiz`·`MasterPath`, 토스 오픈 API(`openapi.tossinvest.com`, OAuth + 100종목 배치), 판정 6,494건 중 `unknown` 530건.
- 차트 VLM fallback 피드백에 대한 답은 '기능 제거'(커밋 `e9055e5`)로 적었습니다. 차트 해석은 틀려도 그럴듯해 보여서 fallback으로 가릴 수 없다는 논리이고, 피셔·막스·소로스 미점수화와 `unknown` 규칙을 같은 판단으로 묶었습니다.
- 원본 피드백 파일은 한글 인코딩이 손상돼(UTF-8 3바이트 중 연속바이트 유실) 남은 바이트로 복원했습니다. 3번 항목의 정확한 표기(게이밍/게이미피케이션 계열)와 개발 순서 첫 줄의 한 단어는 확정하지 못했고, 뜻이 통하는 범위로 적었습니다.
- 부록 B에 문서 불일치 네 건을 기록했고 해당 파일은 고치지 않았습니다. 루트 `CLAUDE.md`의 "예시 데이터" 문단은 실제 `dataSource`가 `sec-toss`라 오래된 기술입니다. README의 테스트 수(79)와 페르소나 챗봇 누락, 이 인계 노트의 "다섯 모델 탭" 기술도 현재와 어긋납니다.
- 검증: data-pipeline 130 pass, web 91 pass, `npm run build` 435/435 정적 페이지 성공. 코드 변경은 없고 `docs/` 파일 추가와 이 노트뿐입니다.

## 2026-08-14 · Codex · 스크리너 정성형 대가 제외 이유 안내

- 스크리너 도입 문구에 피셔·막스·소로스가 빠진 이유를 각각 설명했습니다. 피셔는 정성 탐문, 막스는 시장 수준의 신용·심리, 소로스는 가격·자금 흐름의 변화가 핵심이라 현재 기업 재무 데이터로 점수화하면 철학을 왜곡한다는 경계입니다.
- 세 철학은 스크리너 점수 대신 학습 화면의 자가진단으로 다룬다는 대안도 함께 안내합니다.
- 긴 단일 문단은 짧은 화면 소개와 `점수로 만들지 않은 철학` 안내 영역으로 분리했습니다. 제외 이유는 데스크톱에서 세 칸, 640px 이하에서 이름·이유가 짝지어진 세 줄로 표시합니다.
- 검증: web 테스트 75개 통과, `npm run build` 성공(435 정적 페이지), `/screener/buffett` HTTP 200 및 네 안내 요소 렌더 확인. 인앱 브라우저 목록이 비어 있어 스크린샷 기반 시각 검증은 하지 못했습니다.

## 2026-08-14 · Codex · 학습 장 단계 질문 위치 조정

- 대가별 학습 장에서 공통 단계 질문(`slot.asks`)을 장 제목·인용 문구 뒤가 아니라 단계 탭 바로 아래로 옮겼습니다.
- `/learn/masters/graham/4`의 서버 렌더 HTML에서 `처분` → `언제까지 들고, 무엇이 팔게 하는가` → 장 제목 순서를 확인했습니다.
- 검증: web 테스트 75개 통과, `npm run build` 성공(435 정적 페이지). 인앱 브라우저 목록이 비어 있어 스크린샷 기반 시각 검증은 하지 못했습니다.

## 2026-08-13 · Codex · 그린블랫 원식과 기존 변형 모델 분리

- 기존 5년 평균 세후 ROIC + EBIT/EV 순위 모델을 `greenblatt-inspired` / `Greenblatt-inspired 1.0`으로 이름 붙여 유지했습니다.
- `greenblatt` / `Greenblatt 1.0`은 원래 마법공식의 최신 `EBIT / (유동자산 - 유동부채 + 순유형자산)`와 `EBIT / EV`를 각각 순위 매긴 뒤 합산합니다.
- SEC `PropertyPlantAndEquipmentNet` 계열 태그에서 순유형자산을 수집하고, 없으면 0으로 채우지 않고 원식 점수를 `정보 부족`으로 둡니다.
- 원식은 기존 금융·보험·부동산 제외에 유틸리티 SIC 4900~4999 제외를 추가했습니다. 현재 유니버스가 S&P 500·NASDAQ-100 중복 제거 목록이라 초소형주는 들어오지 않습니다.
- 스크리너에 다섯 모델 탭을 노출하고 `/screener/greenblatt`와 `/screener/greenblatt-inspired`, 종목 상세의 두 모델 전환을 연결했습니다.
- 517종목 전체 `sec-toss` 배치를 새 체크포인트로 실행해 재무 캐시와 `scores.json`을 갱신했습니다. 최종 380종목 중 원식 244종목, 변형식 271종목을 순위화했고 원식에서 유틸리티 29종목을 별도 제외했습니다.
- 검증: data-pipeline 129 pass, web 77 pass, `npm run build` 성공(436 정적 페이지). 네 스크리너·종목 URL HTTP 200 및 각 종목 상세에서 원식/변형식 활성 모델 문구를 확인했습니다.
- 인앱 브라우저 목록이 비어 있어 스크린샷 기반 시각 검증은 하지 못했습니다.

## 2026-08-13 · Codex · 재무 용어 설명 툴팁

- 종목 상세와 스크리너의 기준명·기준식·판정 문장·원자료 지표에서 ROIC, 잉여현금흐름,
  잉여현금흐름 마진·수익률, EBITDA 등 재무 용어를 설명하는 공통 컴포넌트를 추가했습니다.
- 용어는 점선 밑줄과 작은 `?` 표식으로 구분합니다. 데스크톱에서는 hover/focus 시 설명이
  뜨고, 860px 이하에서는 화면 가장자리에 잘리지 않도록 하단 고정 설명으로 전환됩니다.
- 시가총액, PER, PBR, PEG, EV/EBIT, 이익수익률, 이자보상배율, 유동비율,
  부채/자기자본, 연평균 성장률도 같은 용어집에 포함했습니다. 문구는 지표의 정의와 계산 의미만
  설명하며 좋고 나쁨이나 투자 행동은 말하지 않습니다.
- 검증: `npm test` 75개 통과, `npm run build` 통과, 개발 서버의 ADBE 종목 상세와 버핏
  스크리너 HTTP 200 및 렌더 HTML에서 용어 버튼과 ROIC·FCF·EBITDA 설명 확인.
  `.next/static`에 `evEbitMedian5y`가 없어 서버 전용 데이터 번들 경계도 유지됩니다.
- 인앱 브라우저가 연결되지 않아 이번 변경의 스크린샷 검토는 진행하지 못했습니다.

## 2026-08-13 · Codex · 517종목 sec-toss 전체 배치 실행

- `data/universe_us.json`의 517종목을 `--provider sec-toss --mode full`로 실행했습니다.
- SEC 원천 재무 캐시는 429종목이 구성됐고, 품질 게이트를 통과한 380종목이 `apps/web/lib/generated/scores.json`에 반영됐습니다.
- 제외 집계는 공급자 단계 `MISSING_FIELD` 33, `NO_COMMON_YEARS` 43, `NOT_10K` 10, `NOT_LISTED` 2와 품질 단계 `STALE_FINANCIALS` 45, `NON_POSITIVE_IC` 3, `NON_POSITIVE_REVENUE` 1입니다.
- 데이터 기준일은 가격 2026-08-12, 재무 최솟값 2025-03-29입니다.
- 검증: data-pipeline 124 passed, web 75 passed, `npm run build` 통과(435개 정적 페이지).

## 2026-08-13 · Codex · 종목 점수 그래프 기준별 가중치 표시

- `/stocks/LULU?style=lynch` 등 종목 상세의 점수 막대 바로 아래에 기준명과 `가중치 n점 · 비중 n%`를 항상 표시하도록 바꿨습니다.
- 비중은 막대 칸 너비와 동일하게 전체 기준 가중치 합을 분모로 계산합니다. 통과·미충족·판정 불가 표식도 기존 채움·빗금·점선 형태를 그대로 사용합니다.
- 막대 구간의 직접 만든 툴팁과 브라우저 기본 `title` 툴팁이 함께 뜨던 중복을 없앴습니다. 위쪽 툴팁에는 기준명·가중치·비중만 표시하고, 기준식은 막대 아래 각 기준의 설명칸에 표시합니다.
- 좁은 화면에서는 목록이 자연스럽게 한 열까지 줄어들며, 다른 화면의 작은 점수 막대에는 상세 목록을 붙이지 않습니다.
- 검증: `npm test` 75개 통과, `npm run build` 통과, LULU 린치 페이지의 렌더 HTML에서 기준명·가중치·비중 확인. 인앱 브라우저 인스턴스가 없어 스크린샷 검증은 진행하지 못했습니다.

## 2026-08-13 · Codex · sec-toss 실배치 시가총액 공급원 보정

- Toss 공식 종목 마스터 응답에는 발행주식수만 있고 시가총액 필드는 없음을 실응답과 공식 문서로 확인했습니다.
- 시가총액은 별도 키가 필요 없는 Nasdaq 종목 API에서 한 번에 받아오고, SEC 재무·Toss 현재가와 결합하도록 변경했습니다.
- API 시가총액이 없는 종목은 0이나 `주가 × 발행주식수`로 대체하지 않고 제외합니다.
- 실배치 결과 12개 입력 중 DE를 제외한 11개가 통과했고, `data/fundamentals.json`과 `scores.json`을 갱신했습니다. DE는 공통 5개 회계연도를 구성할 수 없어 제외됐습니다.
- 검증: 파이프라인 `124 passed`, 웹 `75 passed`, `npm run build` 통과.

## 2026-08-13 · Codex · 학습 기준일 문구 제거

- 학습 챕터 하단의 2026년 8월 기준 안내 문구와 전용 `currency` 필드를 제거했습니다.
- 저장소 전체에서 해당 문구와 `currency` 사용처가 남지 않은 것을 확인했습니다.
- 검증: `npm test` 75개 통과, `npm run build` 통과. 인앱 브라우저 인스턴스가 없어 시각 확인은 진행하지 못했습니다.

## 2026-08-13 · Codex · 시가총액 API 값 사용

- 데이터 파이프라인의 시가총액 계산을 `price * shares_out`에서 API 제공값으로 바꿨습니다.
- `Fundamentals.market_cap`을 추가했고, `metrics.compute()`는 이 값을 그대로 `Metrics.market_cap`에 복사합니다.
- `SecTossProvider`는 Toss `/api/v1/stocks` 응답의 `marketCap` 계열 필드(`marketCap`, `marketCapitalization`, `marketValue`, `marketValueAmount`)를 읽습니다. 라이브 수집에서 시가총액이 없으면 `MISSING_FIELD`/`BAD_MARKET_CAP`으로 빠지고, 0으로 채우거나 가격×주식수로 대체하지 않습니다.
- `--mode prices` 갱신도 가격과 API 시가총액을 함께 받아 둘 다 있는 종목만 갱신합니다. 하나라도 없으면 기존 캐시 값을 유지해 가격 최신값과 시총 구값이 섞이지 않게 했습니다.
- 오래된 샘플 JSON은 `marketCap`이 없어 테스트·샘플 호환용으로만 `Fundamentals.from_dict()`에 fallback을 남겼습니다. 실데이터 `sec-toss` 경로에는 적용되지 않습니다.
- 검증: `data-pipeline` `python -m pytest -q` 122 pass, `apps/web` `npm test` 75 pass, `npm run build` 통과.

## 2026-08-13 · Codex · 점수 판정 근거 표

- `/stocks/FOX?style=graham` 등 종목 상세 상단에서 종가, 시가총액, 시가총액 순위를 별도 지표 패널로 표시하도록 바꿨습니다. 순위는 현재 `scores.json`의 회사들을 시가총액 내림차순으로 정렬해 계산합니다. FOX는 380종목 중 363위로 렌더링됩니다.
- `/screener/buffett` 등 스크리너의 `점수를 매기는 방식` 카드에서 각 기준에 `가중치 n점 · 비중 x%`를 표시하도록 바꿨습니다. 버핏 1.0 기준으로 3점은 16.7%, 2점은 11.1%, 1점은 5.6%로 렌더링됩니다.
- `/stocks/BR?style=buffett` 등 종목 상세의 점수 카드 아래에 최종 점수 설명을 추가했습니다. 임계값 모델은 `판정 가능한 n점 중 m점을 채워 x점`, 순위 모델은 품질 순위와 가격 순위 합산식을 보여줍니다.
- `Buffett 1.0` 선택 시 점수 카드 안에 `8개 기준, 총 18점` 설명을 추가했습니다. 18점은 좋은 기업 10점, 재무 안정성 3점, 성장 2점, 가격 3점으로 설명하고, 데이터가 없는 기준은 계산에서 제외한다고 안내합니다.
- 기존 `기준과 실제 수치 펼쳐보기`를 `기준식과 실제 판정 데이터 펼쳐보기`로 바꾸고, 표 컬럼을 `기준 / 수식 / 실제 데이터 / 결과`로 정리했습니다. 각 기준의 실제값 문장은 생성된 `criterion.message`, 수식은 `criterion.detail`을 사용합니다.
- 원자료 지표 표는 같은 접힘 영역 아래 `원자료 지표`로 유지했습니다.
- 검증: `npm test` 75 pass, `npm run build` 통과, `http://localhost:3000/stocks/BR?style=buffett` HTTP 200 및 렌더 HTML의 `점수식`, `기준식과 실제 판정 데이터`, `수식`, `실제 데이터` 확인.
- 인앱 브라우저는 `No browser is available`로 직접 스크린샷 검증을 못 했습니다.

## 2026-08-13 · Codex · CriteriaBar 툴팁

- `/stocks/BR?style=buffett`의 기준 막대에서 각 칸 hover/focus 시 `기준명 · 비중 n%` 툴팁이 보이도록 `apps/web/components/CriteriaBar.tsx`와 `apps/web/app/globals.css`를 수정했습니다.
- 비중 퍼센트는 각 칸의 `weight / 전체 weight`로 계산합니다. BR 버핏 기준 예시는 `16.7%`, `11.1%`, `5.6%`로 렌더링됩니다.
- `CriteriaBar`의 타입 import를 클라이언트 번들 규칙에 맞춰 `@/lib/scores`에서 `@/lib/scores.types`로 옮겼습니다.
- 검증: `npm test` 75 pass, `npm run build` 통과, `http://localhost:3000/stocks/BR?style=buffett` HTTP 200 및 렌더 HTML의 `data-tooltip` 확인.
- 인앱 브라우저 목록이 비어 있어 직접 스크린샷 검증은 못 했습니다.

에이전트(Codex ↔ Claude Code)나 사람이 바뀔 때 읽는 파일입니다.
**작업을 마치면 이 파일을 갱신하세요.** 오래된 인계 노트는 없는 것보다 나쁩니다.

마지막 갱신: 2026-08-13 · Codex (개발·빌드 출력 디렉터리 분리)

---

## 지금 하는 중 (중간에 끊겼다면 여기부터)

> 이 절은 작업하면서 계속 갱신합니다. 아래 「끝난 것」이 완료된 것만 적는다면,
> 이 절은 **하다 만 것**을 적습니다. 세션이 갑자기 끊겨도 다음 사람이 어디서부터
> 이어야 할지 알 수 있게. 일이 끝나면 "없음"으로 비웁니다.

**커밋하지 않은 디자인 작업을 계속 진행 중입니다. 현재 변경에는 `/learn`,
`/learn/masters/buffett`의 비례 조정과 버핏 경로의 3구간 캐릭터 배치, 챕터 공통
캐릭터 애니메이션 배치, 해당 대가만 초기화하는 `처음부터 학습하기`가 포함됩니다.
사용자가 전체 디자인 수정을 끝냈다고 명시하기 전에는 커밋·푸시하지 마세요.**

전 서비스 공통 시각 기준은 `docs/design/wisor-design-system.md`에 정리했습니다. 배우기 영역은
그 하위 명세인 `docs/design/learn-design-system.md`를 함께 따릅니다. 다른 메뉴를 만들 때는 상위
문서를 먼저 읽고, 구현 수치를 바꾸면 관련 문서와 이 인계 노트도 같은 작업에서 갱신하세요.

사용자가 실제 브라우저 스크린샷(1920px 데스크톱) 두 장을 보고 지적한 두 가지에서
출발했습니다.

1. 전체 화면 비례, 학습도구 박스 위치, 인물카드 좌우 여백, 캐러셀 잘리는 느낌
2. 대가 세로 토글(레일)이 본문과 너무 멀리 떨어진 느낌

**적용되고 검증까지 끝난 것 (그대로 두세요):**
- `.learn-tools`를 `position:absolute`로 화면 진짜 왼쪽에 붙이던 `@media
  (min-width:1700px)` 블록 삭제 → 문서 흐름 복귀. 실측: 도구박스 x=297,
  로고(wordmark) x=297와 동일선.
- `.master-rail`을 `position:fixed`로 화면 진짜 왼쪽에 붙이던 `@media
  (min-width:1600px)` 블록 삭제. 실측: 레일-본문 간격 481px → 24px.
- `.master-carousel-track`에 `mask-image` 페이드 추가, 시작/끝에서는 그쪽
  방향 페이드를 뺌(`data-at-start`/`data-at-end` 속성, `MasterCarousel.tsx`에서
  세팅). 잘려 보이던 마지막 카드가 "더 있다"는 신호로 읽힘.
- `/learn`의 중복 eyebrow “배우기”를 제거. 현재 위치는 상단 메뉴에서 이미
  표시되므로 제목을 첫 시선으로 만들기 위한 조정입니다. 제목–도구 간격은
  52px→28px, 도구–카드 간격은 32px→52px로 바꿔 도구를 보조 선택으로,
  카드 레일을 핵심 행동으로 분리했습니다. 모바일에서는 상단 메뉴의 가로
  스크롤 동작은 유지하되 스크롤바를 숨겼습니다. 375px에서 문서 가로
  오버플로 없음, 테스트 70 pass 확인.
- 헤더 높이는 60px 그대로 유지하면서 `Wisor`는 1.4rem→1.6rem, 메뉴는
  0.9rem→0.98rem으로 키우고 둘 다 2px 아래로 옮겼습니다. 위쪽 여백을
  늘리면서도 아래 콘텐츠를 밀지 않기 위한 조정입니다. `/learn`의 상단
  패딩만 4px 줄여 제목은 4px 위로 올렸습니다. 375px에서 메뉴는 스크롤되며
  문서 가로 오버플로 없음, 테스트 70 pass 확인.
- `/learn/masters/buffett`에서 큰 CTA를 첫 노드 아래에 두던 배치는 사용자가
  “기괴하다”고 판단해 **폐기**했습니다. 버핏은 `scaleX(-1)` 좌우 반전 상태로
  왼쪽에 고정하며, 노드마다 따라가지 않고 세 번만 이동합니다: 1·2장은 2번 행
  왼쪽(기본보다 아래 30px·왼쪽 20px, 데스크톱은 말풍선 분리를 위해 아래 12px
  추가), 3장은 3번 행 왼쪽(아래 66px·왼쪽 20px, 발끝은 4번 노드 세로 중앙),
  4·5장과 전체 완료는 5번 행
  왼쪽(보정 없음). 첫 구간의 캐릭터와 말풍선 모두 소개 문장 아래에 놓입니다.
  캐릭터 위에는 `1장부터 시작하기` / `n장부터 이어서 하기`를 누를 수 있는 작은
  와인색 말풍선을 둡니다.
- **버핏 캐릭터에는 애니메이션과 hover 이동을 주지 않습니다.** 현재 왼쪽
  위치에서 `scaleX(-1)`로 좌우만 반전해 고정합니다. 새 학습자의
  `1장부터 시작하기`도 정지 상태입니다. 이미 1장을 마친 사용자의
  `2장부터 이어서 하기`(일반화해 `n장부터 이어서 하기`) 말풍선만
  `prefers-reduced-motion`이 아닐 때 1.8초 주기로 3px 위아래 이동합니다.
  말풍선 hover/focus에서는 기존의 2px 상승·옅은 와인 배경 반응을 유지합니다.
- 버핏의 위치는 화면별 임의 좌표가 아니라 위 세 구간의 앵커 행 왼쪽을
  기준으로 제한합니다. 인접한 가장 가까운 노드와 캐릭터 사이의 간격은
  1·2장에서는 두 노드 모두와 데스크톱·태블릿 40px, 모바일 34px 이상입니다.
  3장은 현재 노드와 데스크톱·태블릿 40px, 모바일 34px 이상이고, 4·5장은
  기존값(데스크톱·태블릿 20px, 모바일 14px)을 유지합니다. CTA 말풍선과는
  1·2장 데스크톱에서 소개문 아래 6px·캐릭터 위 12px, 태블릿·모바일에서
  소개문 아래 15.8px 이상입니다. 캐릭터 크기는 소수점 보간 없이 데스크톱
  190px, 1100px 이하 태블릿 150px, 640px 이하 모바일 124px의 세 단계입니다.
  `stand.webp` 원본은 506×720으로 선명도 여유가 충분합니다.
- 1100px 이하에서도 캐릭터는 숨기지 않습니다. 캐릭터 쪽 CTA만 숨기고 같은
  CTA를 현재 노드 위의 작은 말풍선으로 옮깁니다. 큰 모바일 버튼 디자인은
  더 이상 사용하지 않습니다.
- **챕터 읽기·퀴즈·정리 단계의 버핏은 본문 왼쪽에 둡니다.** 버핏의 오른손이
  본문과 선택지를 향하도록 `.chapter-stage`의 열 순서를 캐릭터→본문으로 바꿨고,
  모바일은 기존처럼 콘텐츠 위 중앙 배치를 유지합니다. `guide/great/nope/proud/aha`
  애니메이션은 투명 배경 WebP로 교체했습니다. 각 프레임은 80~90ms로 원본
  60~70ms보다 약 33% 느리고, 5개 합계는 1.45MB입니다. 이전 GIF 5개는 복구용으로
  `C:\Users\Har27\AppData\Local\Temp\wisor-buffett-original-gifs`에 옮겨 두었습니다.
- **챕터 화면은 넓은 화면에서 1080px 프레임을 사용합니다.** 그 안에서 왼쪽 152px은
  캐릭터 전용 거터, 24px은 분리 간격, 나머지는 최대 720px의 읽기·문항 칼럼입니다.
  제목·문항·진행 막대·하단 이동 영역은 본문 시작선에 맞춥니다. 상단 질문에서 진행 막대로
  넘어가는 구간과 하단 이동 영역은 세로 여백을 한 단계 압축했습니다. 캐릭터를 프레임 바깥으로 빼지 않아
  콘텐츠와의 연결감 및 중간 폭 화면의 안전성을 유지합니다. 860px 이하에서는 이 거터를
  없애고 기존의 캐릭터 위·본문 아래 단일 칼럼으로 전환합니다.
- 860px 이하에서는 세로 대가 레일을 한 줄 가로 스크롤 레일로 전환합니다.
  작은 화면에서도 두 줄로 접혀 배너를 아래로 밀지 않으며, 480px 이하에서는
  나머지 대가를 가로 스크롤로 탐색합니다. 640px 이하 헤더는 `Wisor` 보조
  문구를 숨기고 로고·메뉴 크기와 간격을 줄여 보조문구가 둘째 줄에 남던
  기존 디자인 잔재를 제거했습니다.
- 1440/1180/1024/900/768/640/480/360px에서 실제 렌더링을 확인했습니다.
  모든 폭에서 문서 가로 오버플로 없음, 현재 노드와 CTA 유지, 테스트 70 pass,
  layout detector 이상 없음.
- 넓은 화면(861px 초과) 대가 레일은 노란 배너 하단보다 약 22px 아래에서
  시작합니다. 기본 아바타는 44px→50px, 간격은 24px→20px로 조정해 7명의
  대가가 경로의 세로 범위 안에 들어오게 했습니다. 세로 분할선도 첫 아바타부터
  시작하므로 배너 옆에 빈 선이 남지 않습니다. 현재 대가의 확대는 1.2배,
  와인색 외곽선은 2px을 유지합니다. 860px 이하 가로 레일은 기존 배치·간격과
  분할선 숨김을 유지합니다. 640px과 1920px에서 문서 가로 오버플로 없음 확인.
  hover 확대는 포인터가 있는 환경에서만 1.4배로 제한하고, 가로 레일에
  상하·좌우 안전 여백을 확보해 첫 아바타와 이름 툴팁이 잘리지 않게 했습니다.
  510px에서 hover 이미지·툴팁 모두 레일 경계 안에 있음을
  실측했습니다. 테스트 70 pass 및 layout detector 이상 없음.

**시도했다가 되돌린 것 (다시 하지 마세요):**
- 인물카드(`main.png`) 좌우 여백 40px을 줄이려고 `.master-card-art` 높이를
  390→470px, `.master-card` 높이를 560→640px로 올렸습니다. 계산상 여백은
  13px로 줄었지만(검증 완료), **사용자가 "전의 비율이 더 낫다"며 거부** —
  두 값 다 390/560으로 되돌렸습니다. **이 복원 상태가 지금 커밋되지 않은
  작업 트리의 최종 상태입니다.**

**미해결로 열려 있는 것:**
- **인물카드 좌우 여백(40px/쪽)은 여전히 안 고쳐졌습니다.** 원인은 확실합니다 —
  `main.png` 원본 비율 842:1264(0.666)이 art박스 비율 332:378(0.878)보다
  좁아서 `object-fit:contain`이 세로 기준으로 맞추며 좌우가 빕니다. 인물을
  다시 잘라도 서 있는 자세는 비율이 더 좁아질 뿐이라(계산: 0.578) 크롭으로는
  못 고칩니다. 박스 높이를 올리는 안은 방금 거부당했고, 카드 폭을 줄이는
  안도 이전에 거부당했습니다(캐러셀 페이드로 대체). **남은 선택지: 인물
  배경에 색을 넣어 여백을 의도된 디자인처럼 보이게 하거나, 다른 포즈의
  캐릭터 에셋을 새로 그리거나, 아예 여백을 문제로 안 보기로 하거나.** 새
  아이디어가 필요한 지점입니다. 새 후보는 카드 높이·폭을 유지한 채
  `.master-card-main-character`에 `transform: scale(1.1~1.15)`를 적용하는
  방식입니다. 발은 아래에 고정하고 캐릭터를 조금 키워 좌우 여백을 줄이되,
  손·머리 끝이 잘리지 않는지 브라우저에서 확인해야 합니다. 아직 적용하지
  않았고 사용자 확인이 필요합니다.
- **5단계, 상단 제목·메뉴 영역 비율**은 손도 안 댔습니다. masthead가
  로고-완전왼쪽/네비-완전오른쪽 배치라 가운데 968px이 비어 있는데, 이게 진짜
  문제인지 흔한 헤더 형태인지조차 판단을 안 했습니다.

**다음 사람이 할 일:** 위 두 미해결 항목 중 하나를 골라 사용자에게 다시
질문하고 진행하거나, 지금까지 검증된 3건(도구박스·레일·페이드)만 먼저
커밋할지 사용자에게 물어보세요. **로컬 `git commit`은 언제든 해도 됩니다 —
원격 push만 사람 확인 필요.**

---

## 지금 상태

| | |
|---|---|
| 브랜치 | `feat/learn-design-polish` |
| 기준 | `develop` (PR #9·#10 머지 이후 최신) |
| 이전 브랜치 | `feat/learn-duolingo-redesign` — **PR #9로 이미 머지됨.** 더 쓰지 마세요 |
| 커밋 | develop 대비 3개, 전부 문서(AGENTS.md·HANDOFF.md·폰트 정리) |
| 작업 트리 | **깨끗하지 않음** — 위 「지금 하는 중」 참고 |
| 테스트 | 70 pass / 0 fail (최신 미커밋 변경 포함해서 확인함) |
| 정본 경로 | `C:\Users\Har27\Documents\Codex\2026-08-11\new-chat\tokenaires_wisor` |

**PR #9·#10은 이미 머지됐습니다.** 예전에 이 파일이 "PR #9 Draft" 라고
적었던 건 낡은 정보입니다 — 실제로는 2026-08-11 23:56에 naemnaem99가 머지
완료했습니다. `feat/learn-duolingo-redesign` 브랜치는 이제 죽은 브랜치입니다.

## 무엇을 하는 중인가

배우기 화면을 듀오링고를 레퍼런스 삼아 다시 짜고 있습니다. 워런 버핏 캐릭터
에셋이 준비돼 있어 버핏 경로를 먼저 완성하고, 나머지 여섯 대가는 빈 슬롯으로 둡니다.

설계와 계획은 커밋돼 있습니다.

- `docs/superpowers/specs/2026-08-11-buffett-learn-redesign-design.md`
- `docs/superpowers/plans/2026-08-11-buffett-learn-redesign.md`
- `docs/superpowers/specs/2026-08-11-learn-master-carousel-design.md`
- `docs/superpowers/plans/2026-08-11-learn-master-carousel.md`

## 끝난 것

- 캐릭터 에셋 가공 — `apps/web/public/characters/buffett/`
- 팔레트 교체 — 골드 `#FFA000`(듀오링고 초록 자리) · 버건디 `#C2183C`(포인트) · 배경 순백
- 명조 제거, 굵기로 대비
- `content/characters.ts` — 대가별 에셋 유무 판정
- `components/MasterCharacter.tsx` — 캐릭터 한 장
- 대가 선택 화면, 지그재그 노드 경로, 골드 유닛 배너
- 챕터 단계별 캐릭터 반응 (`content/chapterMood.ts`)
- 대가 선택 카드 레일 (`components/MasterCarousel.tsx`)
- 전체 화면 점검 (계획서 Task 9) — 아래 결과 참고

### 전체 화면 점검 결과 (2026-08-11)

- 라우트 19개 전부 200 — 홈 · 배우기 5종 · 대가 3명 · 챕터 2개 · 차트 단원 5개 ·
  스크리너 2개 · 종목 2개 · 연습 · 내 학습
- 옛 토큰(`--plum`, `var(--serif)`) 잔재 없음. 컴포넌트에 하드코딩 hex 없음
- 375px에서 문서 가로 스크롤 없음(6개 화면 확인). 캐러셀과 스크리너 탭이
  가로로 넘치지만 그건 자체 스크롤 컨테이너 안이라 의도된 것
- 키보드 Tab으로 이동하면 버건디 포커스 링이 뜸 — `rgb(194,24,60)` 2px, offset 2px
- 노드 경로: 진도가 없을 때 1번 노드가 `current`, "시작" 말풍선과
  "1장부터 시작하기" CTA 정상
- 고친 것 하나: `app/layout.tsx`가 고운바탕 웹폰트를 계속 받고 있었음.
  `--serif`를 지웠으므로 어디에도 안 쓰이는데 8개 화면 전부에서 내려받고 있었다

## 다음

- 위 「지금 하는 중」의 미해결 두 항목(인물카드 여백, 상단 메뉴 비율)
- 검증된 3건(도구박스·레일·페이드) 커밋 여부 확인
- 스펙에 있으나 아직 안 한 것: 게임 요소(XP·레벨·스트릭·오늘의 목표).
  범위는 아래 「결정된 것」을 보세요.
- README의 「디자인 규칙 하나」 절(빨강·초록 금지)이 PR #10에서 삭제됐습니다.
  사용자가 "그대로 둔다"고 확정했습니다 — **되살리지 마세요.** 규칙 자체는
  `CLAUDE.md`·이 파일의 「결정된 것」에 여전히 살아있고 코드도 그걸 따릅니다.

## 결정된 것 (뒤집기 전에 읽으세요)

- **기준 막대의 충족은 골드입니다. 버건디가 아닙니다.** 기준 막대는 스크리너와
  종목 상세에 나오는데, 그 화면에서 빨강은 가격 방향을 뜻합니다. 버건디는 붉은
  계열이라 충족 표시로 쓰면 제품이 하지 않기로 한 말을 하게 됩니다.
  버건디는 버튼과 배우기 화면 UI에만 씁니다.
- **골드와 황토는 색상환에서 가깝습니다.** 그래서 충족·미충족·미판정은 색이 아니라
  모양으로 갈립니다 — 단색 채움 / 빗금 / 점선 테두리. 이 구분을 없애지 마세요.
- **게임 요소는 파생 가능한 것까지만.** XP·레벨·진행률·완료 표시는 이미 저장된
  `lessonsDone`에서 계산합니다. 스트릭과 오늘의 목표는 `store.ts`에 필드 두 개를
  더하는 선까지. 하트(체력)·젬 상점·리더보드는 범위 밖입니다 — 리더보드는 서버가
  필요한데 Supabase가 아직 연결돼 있지 않습니다.
- **캐릭터 애니메이션 WebP는 높이 360으로 통일돼 있고 폭은 제각각입니다.** 일부러입니다.
  각 애니메이션을 캐릭터에 맞춰 잘라내서, 높이를 고정하면 표정이 바뀌어도 발이 같은 자리에
  놓입니다. 폭을 고정하거나 `object-fit: cover`를 넣으면 이 성질이 깨집니다.

## 함정

- **이 머신에 저장소 사본이 둘 이상 있습니다.** `Downloads\tokenaires_wisor-main`이
  그중 하나입니다. 개발 서버가 그 사본에서 떠서, 고치지도 않은 화면을 검증한 사고가
  있었습니다. 서버를 띄우면 어느 경로에서 도는지 먼저 확인하세요.
- **`~/.codex/.claude/launch.json`이 개발 서버 경로를 정합니다.** 저장소 안의
  `.claude/launch.json`이 아닙니다. 사본을 옮기면 이 파일도 같이 고쳐야 합니다.
- **개발 서버가 살아 있는 동안 `.next`를 지우면 서버가 깨집니다.**
  `Cannot find module './885.js'` 같은 오류가 나면 서버를 멈추고 `.next`를 지운 뒤
  다시 띄우세요. 순서가 중요합니다.
- **개발 서버와 프로덕션 빌드는 출력 디렉터리가 분리돼 있습니다.** `next.config.mjs`가
  `npm run dev`에는 `.next-dev`, `npm run build`에는 `.next`를 사용합니다. 여러 편집
  세션이 동시에 dev/build를 실행하며 manifest와 청크를 지워 500이 반복된 문제를
  막기 위한 설정입니다. 두 명령을 동시에 실행한 상태에서 문제 페이지 30회 HTTP 200,
  테스트 75개와 프로덕션 빌드 통과를 확인했습니다.
- **브라우저 콘솔 버퍼가 재시작 전 오류를 계속 들고 있습니다.** 오류가 오래돼
  보이면 실제 요청 기록과 대조하고 나서 판단하세요.
- **바이너리 diff는 리뷰로 안 잡힙니다.** 캐릭터 이미지의 머리카락이 배경 제거
  과정에서 지워진 적이 있는데, diff에는 `Bin 0 -> 40690 bytes`로만 보였습니다.
  이미지를 바꿨으면 열어서 보세요.

## 미해결

- **`lucide-react` 설치는 보류 중입니다.** 아이콘 라이브러리로 쓰기로 했지만,
  루트 `CLAUDE.md`가 "의존성 추가 금지, 지금 웹은 Next와 React뿐"이라고 못박고
  있습니다. 4인이 공유하는 규칙이라 팀 합의가 필요합니다.
- **`public/characters/buffett/main.png`는 알파 채널이 없습니다.** 흰 배경 위에
  올리면 문제없지만 골드 위에 올리면 흰 사각형이 보입니다. 배치된 자리를 확인하세요.
- **`app/layout.tsx`가 JetBrains Mono를 받지만 아무 데도 안 씁니다.** `--mono`는
  Pretendard로 정의돼 있습니다. 이 브랜치가 만든 문제가 아니라 그 전부터
  그랬으므로 손대지 않았습니다. 정리하려면 `--mono`의 역할부터 정하세요 —
  루트 `CLAUDE.md`는 티커·날짜·수치에 모노를 쓴다고 적고 있는데 실제로는
  등폭 글꼴이 아니라 숫자 열이 안 맞습니다.

## 최신 요구사항 — 대가 학습 순서 재설계 (2026-08-12)

- 팀장님 의견: 배우기 대가 선택 화면에서 **대가 인물/카드가 모두 보이는 구조**가 필요합니다.
  기존 `MasterCarousel`을 제거하고 `MasterGrid`의 4+3 여정형 그리드로 교체했습니다.
- `/learn` 상단의 `주식 기본개념` 및 `대가들의 투자철학 비교` 선택 영역은 제거 대상입니다.
  두 영역 모두 `/learn`에서 제거했습니다.
- 학습 경험은 **워런 버핏을 1단계로 시작해 조지 소로스를 마지막 단계로 이어지는 순차형 여정**으로
  구현했습니다. `MASTERS` 배열의 실제 순서를 그대로 사용하며 카드 번호 `01–07`과 반응형 골드 점선
  리본으로 진행 방향을 표현합니다. 데스크톱은 4+3, 900px 이하는 2열입니다.
- 이 요구사항은 기존 대가별 학습 콘텐츠/퀴즈를 수정하라는 뜻이 아닙니다. 팀장님 담당 영역은 그대로 두고,
  UI 레이아웃·진행 표현·카드/캐릭터 배치만 디자인 범위로 다룹니다.
- 선택된 방향은 **A안(첫 행 4명 + 둘째 행 3명) + 버핏 상반신 크롭 + 소개문구 hover**입니다.
  버핏 `main.png`는 4:3 아트 프레임에서 `object-position: center 10%`로 원본 y 약 66–699를
  보여 줍니다. 좌우 여백은 줄고 얼굴·어깨·손은 유지됩니다.
- 카드 소개문구는 포인터 hover와 키보드 `focus-visible`에서 하단 패널이 열립니다.
  640px 이하와 `hover: none`에서는 항상 표시됩니다. 375/640/900/1280px에서 문서 가로 overflow가
  없고 이미지·본문이 겹치지 않는 것을 브라우저로 확인했습니다.
- 1920×934 전체 화면에서 둘째 행이 잘리지 않도록 데스크톱 상단 padding을 56→40px,
  제목-그리드 간격을 40→32px, 행 간격을 56→36px, 카드 높이를 340→320px로 조정했습니다.
  둘째 행 하단은 893.8px이며 뷰포트 하단 안전 여백은 약 40px입니다. 태블릿·모바일 값은 유지합니다.
- 좁은 화면도 기본 설명을 숨기고 hover/`focus-visible`에서만 열리게 변경했습니다. 640px 이하는
  카드 140px·행 간격 14px, 400px 이하는 카드 136px·행 간격 10px입니다. 475×900에서는 마지막
  카드 하단 776px, 375×812에서는 773px로 7명 전원이 화면 안에 들어오며 가로 overflow는 0입니다.
- 버핏 상반신은 `object-position: center 6%`로 조금 아래에 배치했습니다. 머리 위 여백을 늘리고
  하반신을 더 잘랐으며, 데스크톱과 좁은 화면 모두 얼굴·손이 보이는 것을 확인했습니다.
- 다만 641–1400px에서는 4:3 크롭의 허리 절단선이 눈에 띄어 별도 처리했습니다. 이 구간은
  버핏 원본을 카드 전체 높이에 `object-fit: contain`으로 놓고, 투명→흰색 그라데이션 정보 패널이
  하반신을 자연스럽게 덮습니다. 935px·1260px에서 전신 비율, 이름, hover 소개가 겹치지 않음을
  확인했습니다. 1401px 이상과 640px 이하의 기존 크롭 규칙은 유지합니다.
- 골드 점선 리본의 기존 곡선은 유지하면서 끝만 연장하고 열린 V형 아래 화살표를 추가했습니다.
  데스크톱은 07 카드 왼쪽 아래, 2열 구간은 07 카드 오른쪽 빈 공간에서 끝납니다. 화살표는
  1.8초 주기로 -4px↔5px 이동하며 reduced motion에서는 정지합니다. 640px 이하는 화살표를
  숨겨 압축 카드와 겹치지 않게 했습니다.
- 화살표는 이후 가로폭을 줄이고 세로 줄기와 더 깊은 V를 추가해 아래 방향을 강화했습니다.
  데스크톱 실측 외곽은 약 34×24px이며 리본 끝도 기존보다 약간 더 길게 내렸습니다.
- 테스트 68개 통과, `npx tsc --noEmit` 통과. 캐러셀 전용 테스트 4개는 구현 제거와 함께 삭제되어
  기존 72개에서 68개가 됐습니다. **아직 커밋·push하지 않은 로컬 변경입니다.**

---

## 홈 사이트 소개 보강 (2026-08-13)

- `app/page.tsx`의 헤로와 일곱 투자 철학 사이에 AI 시대에 투자원칙이 필요한 이유와
  Wisor의 학습 방식을 설명하는 섹션을 추가했습니다.
- 핵심 메시지는 “AI가 정보와 답을 늘려도, 확인할 질문과 판단 기준은 사용자가 정한다”입니다.
  첫 시안이 텍스트 위주로 빽빽하다는 피드백을 받아 설명을 두 문장으로 줄이고, 뉴스 요약·실적 해석·
  시장 전망이 ‘나의 투자원칙’의 세 질문으로 모이는 시각적 흐름으로 바꿨습니다.
- 투자원칙이 중요한 이유는 가격과 분위기에 따라 확신이 흔들리는 것을 줄이고, 무엇을 볼지·언제
  생각을 바꿀지·결과에서 무엇을 배울지를 미리 정하는 기준이라는 두 문장으로 설명했습니다.
- Wisor의 역할은 철학 학습 → 기업 적용 → 판단 기록의 세 동작만 남겼습니다. `app/globals.css`에
  홈 전용 반응형 스타일을 추가했으며 새 색·서체·의존성은 없습니다.
- `npm test`: 75 pass, `npm run build`: 성공, `http://localhost:3000/`: 200 및 새 문구 노출 확인.
- 인앱 브라우저가 연결되지 않아 이번 변경의 자동 스크린샷 검토는 하지 못했습니다.

---

## 학습 챕터 위치 표기 개선 (2026-08-13)

- 전제·탐색·검증·처분·실패를 5열 챕터 경로로 모두 노출하고 각 장으로 이동하는 링크로 만들었습니다.
  현재 단계는 진한 채움·하단 표시선·`aria-current`로 구분합니다. 별도의 장 수 표기와 `현재` 문구는
  중복 정보라 제거했습니다.
- `npm test`: 75 pass, `npm run build`: 성공, 버핏 4장 개발 서버 응답에서 새 구조와 문구 확인.
- 인앱 브라우저가 연결되지 않아 자동 스크린샷 검토는 하지 못했습니다.

## 버핏 능력범위 문구 보완 (2026-08-13)

- 버핏 1장 확인 문항의 `원의 크기보다…`를 `능력범위의 크기보다 그 경계가…`로 바꿔
  원의 비유가 무엇을 가리키는지 문항 자체에서 이해되도록 했습니다.

## 그린블랫 Inspired 모델 제거 (2026-08-13)

- 후속 제품 결정으로 `greenblatt-inspired` 모델을 배치 정의·회사별 점수·웹 메뉴·정적 URL에서
  모두 제거했습니다. 이 결정은 이 문서 첫머리의 “기존 변형 모델 유지” 기록을 대체합니다.
- Greenblatt 1.0 원식만 남겼고 README·학습 출처·커버리지의 변형 모델 문구와 전용 분기도
  삭제했습니다.
- 토스 자격 증명이 없는 환경이어서 저장된 429종목 재무 캐시로 공식 `build()`를 실행했습니다.
  기존 유니버스 메타데이터와 가격·재무 기준일을 유지한 채 380종목·4모델로 다시 생성했습니다.
## 그린블랫 탭 보조 문구 정리 (2026-08-13)

- `/screener/greenblatt` 탭의 보조 문구를 내부 모델 버전 `Greenblatt 1.0` 대신 투자 스타일명 `퀀트`로 표시합니다.
- 모델 버전 데이터는 그대로 유지하며, 마스터 정보가 없는 경우에만 탭 보조 문구의 대체값으로 사용합니다.
- 종목 상세의 모델 전환 버튼도 `Greenblatt 1.0` 대신 `그린블랫`으로 표시합니다.

## 일곱 투자 대가 업적 설명 추가 (2026-08-13)

- `/learn/masters/[slug]` 학습 허브에 `투자사에 남긴 것` 섹션을 추가했습니다.
- 일곱 대가 모두 실전·저술·방법론에서 대표 업적 세 가지와 각 근거 자료를 표시합니다.
- 수익률 홍보보다 오늘의 투자자가 이어받은 질문과 도구를 설명하는 방향으로 작성했습니다.
- 상단 `원칙 보기` 링크는 새 섹션으로 바로 이동하는 `업적 보기`로 바꿨습니다.

## README 현재화 (2026-08-13)

- 개발 주차 중심 상태 설명을 현재 제공 기능, 주요 URL, 데이터 규모 중심으로 바꿨습니다.
- Windows를 포함한 실행 방법과 샘플·실데이터 전체 갱신·가격 갱신 명령을 구분했습니다.
- 517개 유니버스, 380개 수록 종목, 네 정량 모델과 실제 데이터 공급원을 명시했습니다.
- 이미 완료된 유니버스 구성은 남은 작업에서 완료로 갱신했습니다.

## 그린블랫 표기 통일 (2026-08-13)

- 사람을 고르는 탭·버튼은 `그린블랫`, 모델 버전을 밝히는 설명·기준일은 `그린블랫 1.0`으로 구분했습니다.
- 배치와 생성 데이터의 `Greenblatt 1.0` 값은 호환성을 위한 내부 값으로 유지하고, 공통 표시 함수에서 한글로 변환합니다.
- README, 학습 각주와 업적 출처의 사용자용 영문 이름도 한글 표기로 통일했습니다.

## 2026-08-20 · Codex · 로그인과 계정 학습 저장 연결

- Supabase Auth를 쿠키 기반 PKCE로 연결하고 `/login`, `/auth/callback`, `/reset-password`를 추가했습니다. Google OAuth와 이메일 가입·로그인·비밀번호 재설정을 지원합니다.
- 비회원의 관심종목·학습노트·진도·퀴즈·기록형 답은 브라우저에 임시 저장하고, 로그인하면 `import_learning_state` RPC로 계정 데이터와 병합합니다. 서버 반영 성공 뒤에만 로컬 원본을 지웁니다.
- 회원 데이터는 RLS가 적용된 `watchlist`, `study_notes`, `lesson_progress`, `quiz_results`, `journal_entries`에 저장합니다. 증분 SQL은 `supabase/migrations/20260820_account_learning_storage.sql`입니다.
- 마이페이지에서 저장 위치와 계정 설정을 확인하고 비밀번호 재설정·로그아웃·계정 삭제를 실행할 수 있습니다. 계정 삭제 API는 `SUPABASE_SECRET_KEY`가 필요합니다.
- 배포 전 Supabase 프로젝트에 마이그레이션을 적용하고 `docs/auth-setup.md`의 OAuth·환경변수를 설정해야 합니다.

## 2026-08-21 · Codex · OAuth 콜백의 운영 주소 유지

- Render 내부 프록시 주소가 `request.url`에 `localhost:3000`으로 반영되므로, 인증 콜백의 성공·실패 응답은 절대 URL 대신 상대 `Location`을 반환합니다.
- 상대 리다이렉트 응답 테스트를 추가했습니다. `python -m pytest -q` 132개와 `npm test` 101개가 통과했고 `npm run build`도 성공했습니다.

## 2026-08-21 · Codex · 배우기 화면 최대 폭 통일

- `/learn` 본문과 `당신은 어떤 기준을 따르고 있나요?` 비교표 섹션의 최대 폭을 기본 헤더와 같은 `1080px`로 맞췄습니다.
- 배우기 페이지에서만 헤더를 `1440px`로 넓히던 예외 규칙을 제거했습니다.
- 일반 메뉴의 활성 밑줄 CSS에서 `.nav-account`를 제외해 로그인 버튼의 아래쪽 테두리가 투명하게 덮이지 않도록 했습니다.
- `npm test` 101개 통과, `npm run build` 성공을 확인했습니다. 브라우저 연결을 사용할 수 없어 자동 스크린샷 검증은 하지 못했습니다.
