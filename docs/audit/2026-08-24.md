# 전체 점검 2026-08-24

f9a069b (feat/windows-batch-scheduler) 시점의 저장소 전체 · 파일 231개

| | 항목 | ❌ | ⚠️ | ✅ | — |
|---|---|---|---|---|---|
| **1부** 절대 하지 말 것 (사실) | 7 | 0 | 2 | 5 | 0 |
| **2부** 클린 코드 5원칙 (판정) | 5 | 3 | 1 | 1 | 0 |

이 커밋의 변경분이 아니라, 이 시점에 저장소에 있던 파일 전부를 봤습니다.
두 줄을 더하지 않습니다 — 1부는 기계가 센 사실이고 2부는 AI 판정이라, 합치면
"틀리지 않는 것"과 "틀릴 수 있는 것"이 한 숫자에 섞입니다.

작업 트리가 깨끗하지 않은 상태에서 돌렸습니다(`feat/windows-batch-scheduler`에
수정 3건·미추적 7건). 점검은 `HEAD` 기준이라 그 작업은 이 보고서에 들어 있지 않습니다.


## 1부 — 절대 하지 말 것 (기계가 센 것, 틀리지 않음)

❌ 0 · ⚠️ 2 · ✅ 5

| # | 항목 | 결과 |
|---|---|---|
| 1 | 없는 값을 0으로 채우기 | ✅ pytest 4개가 봄 (여기서 재검사 안 함) |
| 2 | 주식 화면에 빨강·초록 | ✅ 0건 |
| 3 | scores.json 직접 수정 | ✅ generatedAt이 안 바뀐 채 파일만 바뀐 커밋 0개 |
| 4 | 컴포넌트에서 localStorage 직접 호출 | ✅ 0건 |
| 5 | 클라이언트에서 lib/scores.ts import | ✅ check.yml이 봄 (여기서 재검사 안 함) |
| 6 | 의존성 추가 | ⚠️ 3개 → **5개**. `@supabase/ssr`·`@supabase/supabase-js` 추가됨 — 2부 5번 참조 |
| 7 | 금지 문구 | ⚠️ 검사 밖 영역에 7건 → **10건** — 2부 3번 참조 |

1번을 보는 테스트: `test_missing_data_becomes_unknown_not_fail`,
`test_negative_equity_makes_pbr_unknown_not_a_negative_ratio`,
`test_negative_equity_makes_debt_to_equity_unknown`,
`test_zero_interest_expense_is_unknown_not_infinite_coverage`

6번 실제 목록: `dep: @supabase/ssr, @supabase/supabase-js, next, react, react-dom` /
`dev: @types/node, @types/react, @types/react-dom, typescript`


## 2부 — 클린 코드 5원칙 (AI 판정, 틀릴 수 있음)

❌ 3 · ⚠️ 1 · ✅ 1

**1. 기능에 맞는 테스트가 있는가?** ⚠️

짝이 되는 테스트 파일이 없는 소스가 14개 → **24개**(TS 19 · PY 5)로 늘었습니다.
늘어난 쪽은 확인해보니 대부분 문제가 아닙니다.

```
lib/supabase/client.ts       8줄    createBrowserClient 한 줄 호출
lib/supabase/config.ts       9줄    환경변수 두 개 읽기
lib/supabase/server.ts      26줄    createServerClient + 쿠키 전달
lib/supabase/middleware.ts  26줄    같음
lib/useInView.ts            30줄    IntersectionObserver 래퍼
```

다섯 다 30줄 이하이고 외부 라이브러리 호출을 감싸기만 합니다. 나머지는
`content/curriculum/*.ts` 10개인데 `validate.test.ts`가 한꺼번에 봅니다.

**실제로 비어 있는 곳은 지난 회차와 똑같은 둘이고, 고쳐지지 않았습니다.**

**`lib/personaApi.ts` 157줄** — 파이썬 서버의 HTTP 응답을 받아 화면에 넘기는데 테스트가 없습니다.
→ 서버 응답 형태가 바뀌면 챗봇 화면이 조용히 깨지고, 아무도 그 사실을 모릅니다.

**`lib/scores.ts`의 `FINANCIAL_RANGE`** — 종목별 재무 기준일을 정렬해 최솟값·최댓값을 뽑는
유일한 계산인데 테스트가 없습니다.
→ 모든 점수 화면에 나가는 날짜라, 틀려도 화면은 멀쩡해 보입니다.

⚠️인 이유는 위험한 둘이 그대로 남았을 뿐 새로 나빠지지는 않았기 때문입니다.

**2. 지금 한 가지만 고칠 수 있는 구조인가?** ❌ (지난 회차 ⚠️에서 내려감)

**❌의 근거는 아래 (나) 하나입니다.** (가)는 지난 회차와 같은 상태이고, 악화되지 않았습니다.

**(가) `components/MyLearning.tsx` — 지난 회차 지적 그대로입니다.**

488줄 → 503줄. 41행의 `export default function`이 503행까지 **한 번도 끊기지 않고**
이어집니다(파일 안 최상위 함수가 이것 하나입니다). 그 안에 **인라인으로 펼쳐진 섹션이
넷**이고, 이건 지난 회차와 같습니다.

```
155행  퀴즈 결과
283행  90일 전에 쓴 답입니다
327행  관심종목
407행  종목 학습노트
498행  계정 설정          ← 늘어난 15줄. 여기만 다른 방식이다
```

늘어난 15줄은 확인해보니 **문제가 아니라 오히려 올바른 형태**입니다. 496~501행이
제목 한 줄과 `<AccountSettings />` 호출뿐이고, 실제 내용 192줄은
`components/AccountSettings.tsx`라는 별도 파일에 있습니다. 나머지 네 섹션과 달리
이 하나는 안에 펼쳐져 있지 않습니다.

(초안에서 이 섹션을 "다섯 번째 책임이 들어왔다"고 적었다가, 490~503행을 실제로 읽고
고쳤습니다. `<h2>` 위치만 세고 판정한 잘못입니다.)

남는 문제는 지난 회차와 똑같습니다 — 인라인 네 섹션 때문에 "관심종목만 고쳐줘"라고
시키면 AI가 462줄을 통째로 읽고 고치게 되고, 나머지 세 섹션이 같이 흔들려도 diff로는
알아채기 어렵습니다. 지난 회차대로 **⚠️ 수준이지 ❌는 아닙니다** — "몇 줄부터 쪼개야
하는가"가 문서에 없고, 지금 당장 깨진 곳도 없습니다. `AccountSettings.tsx`가 나머지
넷을 어떻게 빼면 되는지 보여주는 본보기입니다.

**(나) `app/globals.css` 5,316줄 — 이번 회차에 처음 들여다봤습니다. ❌는 이것입니다.**

지난 회차는 `.ts`·`.tsx`만 셌기 때문에 CSS가 한 번도 보고서에 나온 적이 없습니다.
경로 범위(`apps/web/`)는 그대로이고, 기계 검사 명령이 CSS를 안 셀 뿐입니다.

```
app/globals.css      5,316줄 · 셀렉터 630개 · 섹션 27개   layout.tsx가 전역 로드
app/home-v5.css      2,006줄 · 셀렉터 110개              page.tsx만 로드
app/master-tabs.css    455줄 · 셀렉터  42개              learn/masters/[slug]만 로드
```

`globals.css` 하나가 하는 일을 한 문장으로 말할 수 없습니다 — 디자인 토큰(1행),
레이아웃(117), 활자(230), 기준 막대(386), 종목 줄(661), 홈 헤로(1357),
일곱 스타일 비교표(1673), 학습 경로 지그재그 노드(2437), 출처 각주(3098)가
한 파일에 있습니다. 전역이라 **화면 하나를 고치려고 이 파일을 열면 나머지 열 개 화면이
전부 사정권에 들어옵니다.**

→ 이게 왜 위험하냐면, **테스트도 빌드도 레이아웃이 깨진 걸 못 잡습니다.**
`pytest`·`npm test`·`npm run build` 셋 다 통과하면서 화면이 망가질 수 있는 유일한
자리입니다. 코드를 못 읽는 사람은 diff로도, 테스트 결과로도 알 수 없습니다.

되돌리면 안 되는 이유가 파일 안에 이미 적혀 있습니다 — "min-width를 두지 않는다"(1705행),
"1px는 실제 높이가 아니라 자식이 height:100%를 계산할 수 있게 하는 기준값"(1789행),
2018·2127행의 미디어쿼리 순서. **캐스케이드 순서에 의존하는 자리가 여럿이라 그냥
잘라 나누면 조용히 깨집니다.** 쪼갠다면 화면별로 눈으로 확인할 항목을 먼저 적어야 합니다.

**참고 — 큰데 문제가 아닌 파일들.** 줄 수로 판정하지 않았습니다.
`sec_toss.py` 670줄은 "외부에서 재무·가격을 가져온다", `masters.ts` 472줄은 "대가 목록 데이터",
`persona_explain/server.py` 527줄은 HTTP 경로 정의가 대부분이고 판단은
`chat.py`·`explain.py`·`safety.py`로 나가 있습니다. `lib/store.ts` 461줄은 함수 19개가
관심종목·학습노트·진도·퀴즈·저널로 갈라지지만, CLAUDE.md가 "저장은 lib/store.ts를 거칩니다"라고
**의도적으로 한 문으로 모아둔 곳**(Supabase 교체 지점)이라 한 문장으로 말할 수 있습니다.

**3. 같은 일을 하는 코드가 한 곳에만 있는가?** ❌ (지난 회차와 같음, 고쳐지지 않음)

컴포넌트 사이에 복붙된 코드 블록은 없습니다. 문제는 **같은 제품 규칙("권유하지 않는다")을
검사하는 필터가 세 군데 있고, 세 목록이 여전히 전부 다른** 것입니다. 이번에 셋 다 열어
대조했고, 지난 회차 이후 하나도 좁혀지지 않았습니다.

```
data-pipeline/wisor_data/styles/base.py   BANNED_PHRASES  단어 11개
   매수 매도 목표가 손절 추천 보장 확실 오를 내릴 급등 바닥

apps/web/content/curriculum/validate.ts   BANNED          구 6개
   "지금 사" "지금 파" "손절하" "추천합니다" "확실합니다" "보장합니다"

persona_explain/safety.py                 _RULES          정규식 5줄
   위 둘에 없는 말: 익절 · 적정주가 · 천장 · 저평가 · 고평가 · 매수관점
```

→ 새 금지어를 한 곳에만 추가하면 나머지 두 화면은 그대로 통과시킵니다.
지금도 `저평가`는 챗봇에서만 막힙니다 — 점수 화면과 커리큘럼에서는 그대로 나갑니다.
(CLAUDE.md 클린 코드 5원칙 3 — "같은 로직이 두 곳에 있으면 나중에 AI가 한 곳만 고치고
나머지를 놓쳐서, 눈에 안 보이는 불일치가 생깁니다")

반론도 지난 회차와 같이 적어둡니다 — 세 곳은 언어와 실행 시점이 달라(파이썬 배치 /
TS 빌드 / 파이썬 서버) 함수로 뽑을 수가 없습니다. 현실적인 최소한은 **목록을 데이터
파일(JSON) 하나로 두고 셋 다 읽는 것**이고, 그것도 무거우면 **왜 목록이 다른지를
적어두는 것**입니다.

그리고 세 목록 중 **어느 쪽도 `components/`와 `content/masters.ts`를 보지 않습니다**(1부 7번).
거기 걸린 10건은 확인해보니 **10건 전부 오탐이거나 오히려 규칙을 지키는 문장**이었습니다.

```
StockLenses.tsx:69       "기업을 좁히는 출발점이며, 매수·매도 판단이 아닙니다"   ← 규칙을 지키는 문장 (2건)
masters.ts:82            "과거의 안정성이 앞으로를 보장하지 않습니다"            ← 부정문
masters.ts:96            "'앞으로 얼마나 오를까'를 맞히려 하지 않았습니다"       ← 설명하려는 인용
masters.ts:176           "'아는 제품'이 곧 매수 근거는 아니라고 선을 그었습니다" ← 부정문
masters.ts:239           "불확실성"                                            ← '확실'이 걸린 것
DuoQuiz.tsx:9            "정오를 보여주고"                                      ← '정오'+'를'
HomeHero.tsx:77,80       "노란 바닥이 두 줄을 다 받쳐야"                        ← CSS 주석 (2건)
HomeMasters.tsx:225      "아래를 화면 바닥에 맞춰"                              ← JSX 주석
```

지난 회차 7건에서 늘어난 3건은 전부 홈 화면 주석의 "바닥"(화면 아래쪽)입니다.
**기계 grep을 판정으로 쓰면 안 되는 이유**가 여기 있습니다.

**4. 처음 보는 방식이 들어갔는가?** ✅

이 저장소에서 여기서만 쓰는 방식은 찾지 못했습니다. 이번에 확인한 것 둘입니다.

- 스타일 모듈 넷이 전부 `base.py`의 `Criterion` 구조를 그대로 씁니다 —
  `buffett.py` 8개 · `graham.py` 6개 · `lynch.py` 5개 · `greenblatt.py` 2개,
  넷 다 `base`를 import합니다. 자기만의 점수 계산을 따로 만든 모듈이 없습니다.
- 이번에 새로 들어온 `lib/supabase/` 4개 파일이 전부 `@supabase/ssr`의
  `createBrowserClient`/`createServerClient` 표준 호출입니다. 자체 래퍼나
  전역 싱글턴 같은 변형이 없습니다.

`import`에 `.ts`를 붙인 곳(32건)과 안 붙인 곳(35건)이 섞여 있는데 이유가 있습니다 —
`npm test`가 `node --test`로 직접 돌리는 파일은 확장자가 필요하고, Next만 거치는 파일은
필요 없습니다. 규칙이 있으니 4번은 ✅이고, 그 규칙이 문서에 없다는 것은 5번 문제입니다.

**5. 왜 그렇게 만들었는지가 영역마다 기록으로 남아 있는가?** ❌ (지난 회차 ⚠️에서 내려감)

지난 회차는 "기록이 없다"였습니다. 이번에는 **기록이 틀린 자리가 생겼습니다.**

**(가) 루트 CLAUDE.md 150행이 사실과 다릅니다.**

```
문서:         "지금 웹은 Next·React뿐이고 그 상태를 유지합니다"
package.json:  @supabase/ssr, @supabase/supabase-js, next, react, react-dom
```

→ 없는 기록보다 나쁩니다. 세션을 열면 AI가 이 문장을 사실로 읽고, 이미 들어와 있는
Supabase를 "규칙 위반"으로 지적하거나 지우려 듭니다. 2번 담당의 정식 영역인데도요.
(CLAUDE.md 클린 코드 5원칙 5 — "왜 그렇게 했는지가 기록으로 남았는가")

**(나) `persona_explain/`이 여전히 루트 CLAUDE.md에 없습니다 — 지난 회차 지적 그대로.**

"저장소 구조와 경계" 절은 `apps/web`·`data-pipeline`·`supabase`·`docs` 넷만 적는데,
실제로는 다섯 번째 영역이 있고 파일이 19개(그중 파이썬 13개·3,321줄)입니다. 지난 회차
숫자와 같습니다 — 그때도 19개였고, 줄어든 게 아닙니다. "4인 역할과 소유권" 표에도
담당이 없습니다(4번 자리는 공석으로 비어 있습니다).
→ 세션을 열면 이 영역이 존재한다는 사실 자체가 컨텍스트에 안 들어갑니다.

**(다) 화면은 9개 → 11개로 늘었는데 설계 문서는 여전히 배우기 화면 얘기입니다.**

```
기록이 없는 화면·영역
  app/screener/[style]                  스크리너
  app/stocks/[ticker]                   종목 상세
  app/me                                내 학습
  app/login · app/reset-password        이번에 늘어난 둘
  data-pipeline/wisor_data/styles/      버핏·그레이엄·린치·그린블랫 점수 모델
  persona_explain/                      챗봇 서버 19개 파일
```

→ 다음에 AI에게 스크리너나 점수 모델을 고치라고 할 때 넣어줄 맥락이 없습니다.
지금은 코드를 읽고 추측하는 수밖에 없고, 추측이 틀리면 비개발자는 알아채지 못합니다.

같은 이유로 아래 셋도 기록이 없습니다 — 금지어 목록이 왜 셋으로 갈렸는지(2부 3번),
import 확장자를 언제 붙이는지(2부 4번), `home-v5.css`의 "v5"가 무엇의 5번째인지.


## 함께 본 것 (판정 대상 아님)

점검 범위 밖이지만 눈에 띈 것들입니다. 규칙대로 **언급만 하고 고치지 않았습니다.**

- **`services/chart-api/`** — git에 추적되지 않는 로컬 잔재입니다(차트 교육 기능 제거 흔적,
  CLAUDE.md 4번 담당 공석 사유와 일치). 저장소에는 없으므로 다른 사람 화면에는 안 보입니다.
- **`.gitignore`의 로그 규칙이 실제 파일과 안 맞습니다** — 규칙은 `.dev-server.*.log`(앞에 점)인데
  실제로 생기는 파일은 `apps/web/dev-server.err.log`·`persona_explain/chatbot-server.out.log`입니다.
  네 개 다 추적 대상으로 남아 `git status`에 계속 뜹니다.
- **`persona_explain` 테스트 1개 실패** — `scores.json`의 `magicFormulaRoc`가
  `scores_source.py`의 `SCORES_KEY_TO_METRIC`에 없는 계약 불일치(HANDOFF.md 13행).
  이 스킬은 테스트를 돌리지 않으므로 재확인하지 않았고, 문서에 적힌 내용을 옮깁니다.


## 지난 회차와 달라진 것

`docs/audit/2026-08-20.md`(4일 전, 파일 186개)와 비교했습니다. 파일이 **231개**로 45개 늘었습니다.

| | 08-20 | 08-24 | |
|---|---|---|---|
| 1부 ❌ / ⚠️ / ✅ | 0 / 1 / 6 | 0 / 2 / 5 | 의존성이 3개 → 5개로 늘어 ⚠️ 하나 추가 |
| 2부 ❌ / ⚠️ / ✅ | 1 / 3 / 1 | 3 / 1 / 1 | ⚠️ 둘이 ❌로 내려감 |

**고쳐진 것: 없습니다.** 지난 회차 지적 5개가 4일 뒤에도 전부 그대로입니다.

**실제로 코드가 나빠진 것은 하나뿐입니다.**

- **원칙 5 ⚠️ → ❌** — Supabase 두 패키지가 들어오면서 CLAUDE.md 150행이 **틀린 문장**이
  됐습니다. 지난 회차의 "기록이 없다"와 달리 이건 AI를 잘못된 방향으로 끕니다.

**원칙 2의 ⚠️ → ❌는 코드가 나빠져서가 아닙니다.** 이 4일 사이에 `MyLearning.tsx`는
15줄 늘었지만 그 15줄은 제대로 분리된 형태였고, 지난 회차 지적(인라인 네 섹션)은
좋아지지도 나빠지지도 않았습니다. 등급이 내려간 이유는 **CSS 7,777줄을 이번에 처음
들여다봤기 때문**입니다. 경로 범위(`apps/web/`)는 처음부터 CSS를 포함했는데, 기계 검사
명령이 `.ts`·`.tsx`만 세느라 지난 회차 보고서에 한 번도 나오지 않았습니다.

→ **회차끼리 숫자를 비교할 때 원칙 2는 빼고 보셔야 합니다.** 그리고 다음 회차에 이게
다시 안 세어지면 숫자가 또 흔들립니다. `.claude/skills/whole-check/SKILL.md`의
원칙2 검사 명령에 `.css`를 넣을지가 결정 사항으로 남습니다(검수는 고치지 않으므로
여기서는 적어두기만 합니다).
